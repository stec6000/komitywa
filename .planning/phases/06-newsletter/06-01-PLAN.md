---
phase: 06-newsletter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - newsletter/__init__.py
  - newsletter/apps.py
  - newsletter/models.py
  - newsletter/forms.py
  - newsletter/emails.py
  - newsletter/admin.py
  - newsletter/urls.py
  - newsletter/views.py
  - newsletter/migrations/__init__.py
  - newsletter/migrations/0001_initial.py
  - newsletter/tests/__init__.py
  - newsletter/tests/test_models.py
  - newsletter/tests/test_views.py
  - newsletter/tests/test_emails.py
  - backend/settings.py
  - backend/urls.py
  - templates/base.html
  - templates/includes/_newsletter_signup.html
autonomous: true
requirements:
  - NEWS-01
  - NEWS-02
  - NEWS-03

must_haves:
  truths:
    - "Newsletter app is registered in INSTALLED_APPS and migrations run cleanly"
    - "Subscriber model stores email, tokens, confirmation state, and unsubscribe state"
    - "Newsletter signup form renders on every page above the footer"
    - "Newsletter URLs are resolvable without NoReverseMatch errors"
    - "Admin can view and filter subscribers"
  artifacts:
    - path: "newsletter/models.py"
      provides: "Subscriber model with dual tokens"
      contains: "class Subscriber"
    - path: "newsletter/forms.py"
      provides: "Newsletter signup form with RODO consent"
      contains: "class NewsletterSignupForm"
    - path: "newsletter/emails.py"
      provides: "Confirmation email sending"
      contains: "def send_confirmation_email"
    - path: "newsletter/admin.py"
      provides: "Subscriber admin interface"
      contains: "class SubscriberAdmin"
    - path: "newsletter/urls.py"
      provides: "Newsletter URL patterns"
      contains: "app_name"
    - path: "templates/includes/_newsletter_signup.html"
      provides: "Newsletter section template"
      min_lines: 10
  key_links:
    - from: "templates/base.html"
      to: "templates/includes/_newsletter_signup.html"
      via: "{% include %} between </main> and footer"
      pattern: 'include.*_newsletter_signup'
    - from: "backend/settings.py"
      to: "newsletter"
      via: "INSTALLED_APPS"
      pattern: '"newsletter"'
    - from: "backend/urls.py"
      to: "newsletter/urls.py"
      via: "include"
      pattern: 'include.*newsletter.urls'
---

<objective>
Create the newsletter Django app with Subscriber model, forms, email sending, admin, URL routing, test scaffold, and the global newsletter signup section in base.html.

Purpose: Establishes all backend infrastructure and the globally-visible signup form so Plan 02 can implement the view logic and page templates.
Output: Working newsletter app registered in Django with model, form, email helper, admin, URLs, tests, and signup section visible on every page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-newsletter/06-CONTEXT.md
@.planning/phases/06-newsletter/06-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs to understand -->

From backend/settings.py (INSTALLED_APPS, line 42-62):
```python
INSTALLED_APPS = [
    "django.contrib.admin",
    ...
    "accounts",
    "core",
    "recipes",
    "shop",
]
```

From backend/urls.py (line 8-17):
```python
urlpatterns = [
    path("admin/", admin.site.urls),
    ...
    path("przepisy/", include("recipes.urls", namespace="recipes")),
    path("", include("core.urls")),
    path("", include("shop.urls")),
]
```

From templates/base.html (line 33-37):
```html
    <main id="main-content">
        {% block content %}{% endblock %}
    </main>

    {% include "includes/_footer.html" %}
```

From shop/emails.py (email pattern):
```python
from django.conf import settings
from django.core.mail import EmailMessage

email = EmailMessage(
    subject="...",
    body=body,
    from_email=settings.DEFAULT_FROM_EMAIL,
    to=[recipient_email],
)
email.send(fail_silently=False)
```

From static/css/main.css (CSS custom properties):
```css
:root {
    --kk-sage: #9CAF88;
    --kk-olive: #6B7F5E;
    --kk-olive-dark: #4A5A40;
    --kk-cream: #F5F0E8;
    --kk-warm-white: #FDFBF7;
    --kk-beige: #D4C5A9;
    --kk-earth: #8B7355;
    --kk-text: #3D3D3D;
    --kk-text-light: #6B6B6B;
    --kk-font-heading: "Lora", serif;
    --kk-font-body: "Nunito", sans-serif;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Newsletter app scaffold -- model, forms, emails, admin, URLs, settings, and test scaffold</name>
  <files>
    newsletter/__init__.py,
    newsletter/apps.py,
    newsletter/models.py,
    newsletter/forms.py,
    newsletter/emails.py,
    newsletter/admin.py,
    newsletter/urls.py,
    newsletter/views.py,
    newsletter/migrations/__init__.py,
    newsletter/tests/__init__.py,
    newsletter/tests/test_models.py,
    newsletter/tests/test_views.py,
    newsletter/tests/test_emails.py,
    backend/settings.py,
    backend/urls.py
  </files>
  <read_first>
    backend/settings.py,
    backend/urls.py,
    shop/emails.py,
    shop/forms.py,
    shop/admin.py,
    .planning/phases/06-newsletter/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test: Subscriber.generate_token() returns a string of length >= 32
    - Test: Subscriber creation with email sets is_confirmed=False, is_unsubscribed=False
    - Test: Subscriber.__str__() returns the email address
    - Test: Subscriber.is_confirmation_expired() returns True when confirmation_sent_at is older than 24h
    - Test: Subscriber.is_confirmation_expired() returns False when confirmation_sent_at is within 24h
    - Test: Subscriber.is_confirmation_expired() returns True when confirmation_sent_at is None
    - Test: Subscriber email field has unique=True constraint
    - Test: NewsletterSignupForm valid with email + consent_newsletter=True
    - Test: NewsletterSignupForm invalid without consent_newsletter
    - Test: NewsletterSignupForm invalid without email
    - Test: send_confirmation_email sends one email with correct subject containing "Potwierdz zapis"
    - Test: send_confirmation_email body contains confirmation URL with token
    - Test: send_confirmation_email body contains unsubscribe URL with unsubscribe_token
    - Test: send_confirmation_email updates confirmation_sent_at on subscriber
    - Test: POST to newsletter:subscribe with valid data creates Subscriber and redirects to newsletter:check_email (302)
    - Test: POST to newsletter:subscribe with already-confirmed email redirects to newsletter:check_email (does not create duplicate)
    - Test: POST to newsletter:subscribe with pending (unconfirmed) email resends confirmation and redirects
    - Test: POST to newsletter:subscribe with unsubscribed email resets is_unsubscribed and sends confirmation
    - Test: GET to newsletter:subscribe redirects to home
    - Test: GET to newsletter:confirm with valid unexpired token sets is_confirmed=True
    - Test: GET to newsletter:confirm with expired token shows link_expired template
    - Test: GET to newsletter:confirm with invalid token returns 404
    - Test: GET to newsletter:unsubscribe with valid token for confirmed subscriber sets is_unsubscribed=True
    - Test: GET to newsletter:unsubscribe with already-unsubscribed token renders "already unsubscribed" message
    - Test: GET to newsletter:unsubscribe with invalid token returns 404
  </behavior>
  <action>
    1. Create `newsletter/` app directory with `__init__.py` and `apps.py`:
       - `apps.py`: `class NewsletterConfig(AppConfig)` with `name = "newsletter"`, `verbose_name = "Newsletter"`

    2. Create `newsletter/models.py` with `Subscriber` model (per D-04, Claude's Discretion in CONTEXT.md):
       - `email = models.EmailField(unique=True)`
       - `is_confirmed = models.BooleanField(default=False)`
       - `confirmation_token = models.CharField(max_length=64, unique=True)`
       - `confirmation_sent_at = models.DateTimeField(null=True, blank=True)`
       - `unsubscribe_token = models.CharField(max_length=64, unique=True)`
       - `is_unsubscribed = models.BooleanField(default=False)`
       - `created_at = models.DateTimeField(auto_now_add=True)`
       - `class Meta: verbose_name = "Subskrybent"`, `verbose_name_plural = "Subskrybenci"`
       - `def __str__(self): return self.email`
       - `@staticmethod def generate_token(): return secrets.token_urlsafe(32)`
       - `def is_confirmation_expired(self):` returns True if `confirmation_sent_at` is None or older than 24h

    3. Create `newsletter/forms.py` with `NewsletterSignupForm(forms.Form)` (per D-02):
       - `email = forms.EmailField(label="Adres email", widget=forms.EmailInput(attrs={"class": "form-control", "placeholder": "Twoj adres email"}))`
       - `consent_newsletter = forms.BooleanField(label='Wyrazam zgode na otrzymywanie newslettera. <a href="/polityka-prywatnosci/" target="_blank">Polityka prywatnosci</a>', widget=forms.CheckboxInput(attrs={"class": "form-check-input"}))`

    4. Create `newsletter/emails.py` with `send_confirmation_email(subscriber, request)` (per D-04, D-08):
       - Build `confirm_url` using `request.build_absolute_uri(reverse("newsletter:confirm", args=[subscriber.confirmation_token]))`
       - Build `unsubscribe_url` using `request.build_absolute_uri(reverse("newsletter:unsubscribe", args=[subscriber.unsubscribe_token]))`
       - Email subject: `"Potwierdz zapis do newslettera -- Kuchenna Komitywa"`
       - Plain text body with warm Polish tone (match shop/emails.py pattern)
       - Include confirm link, 24h validity note, ignore-if-not-you note, unsubscribe link at bottom
       - Update `subscriber.confirmation_sent_at = timezone.now()` and save before sending
       - Use `EmailMessage` from `django.core.mail`

    5. Create `newsletter/admin.py` with `SubscriberAdmin` (per Claude's Discretion):
       - `@admin.register(Subscriber)`
       - `list_display = ["email", "is_confirmed", "is_unsubscribed", "created_at"]`
       - `list_filter = ["is_confirmed", "is_unsubscribed"]`
       - `search_fields = ["email"]`
       - `readonly_fields = ["confirmation_token", "unsubscribe_token", "confirmation_sent_at", "created_at"]`

    6. Create `newsletter/urls.py` with `app_name = "newsletter"`:
       - `path("newsletter/zapisz/", views.subscribe, name="subscribe")`
       - `path("newsletter/sprawdz-email/", views.check_email, name="check_email")`
       - `path("newsletter/potwierdz/<str:token>/", views.confirm, name="confirm")`
       - `path("newsletter/wypisz/<str:token>/", views.unsubscribe, name="unsubscribe")`

    7. Create `newsletter/views.py` with stub views that implement full logic:
       - `subscribe(request)`: POST-redirect-GET per D-03, D-05; handles new subscriber, confirmed duplicate (silent redirect), pending duplicate (resend), unsubscribed re-subscribe (per Pitfall 6 in RESEARCH.md); wrap get_or_create in try/except IntegrityError (Pitfall 1); GET redirects to "home"
       - `check_email(request)`: Renders `newsletter/check_email.html`
       - `confirm(request, token)`: GET finds Subscriber by confirmation_token or 404; if expired shows `newsletter/link_expired.html`; if valid sets is_confirmed=True, saves, renders `newsletter/confirmed.html`
       - `unsubscribe(request, token)`: GET finds Subscriber by unsubscribe_token or 404; if already unsubscribed renders "already unsubscribed" variant; otherwise sets is_unsubscribed=True, saves, renders `newsletter/unsubscribed.html` (per D-06, D-07)

    8. Register in `backend/settings.py`: Add `"newsletter"` to INSTALLED_APPS after `"shop"`

    9. Register in `backend/urls.py`: Add `path("", include("newsletter.urls")),` before the core catch-all `path("", include("core.urls"))`

    10. Create `newsletter/tests/__init__.py` (empty)

    11. Create `newsletter/tests/test_models.py`: Tests for Subscriber model creation, __str__, generate_token, is_confirmation_expired (None, expired, valid)

    12. Create `newsletter/tests/test_views.py`: Tests for all view behaviors listed in <behavior> above. Use `django.test.TestCase`, `self.client.post()`/`.get()`, `django.core.mail.outbox` for email assertions.

    13. Create `newsletter/tests/test_emails.py`: Tests for send_confirmation_email -- uses `RequestFactory` to create request, checks `mail.outbox` for subject, body content (confirm URL, unsubscribe URL), and that confirmation_sent_at is updated.

    14. Create migration: Run `python3 manage.py makemigrations newsletter`

    15. Run migrations: `python3 manage.py migrate`
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && python3 manage.py test newsletter -v2</automated>
  </verify>
  <acceptance_criteria>
    - `newsletter/models.py` contains `class Subscriber(models.Model):`
    - `newsletter/models.py` contains `email = models.EmailField(unique=True)`
    - `newsletter/models.py` contains `confirmation_token = models.CharField(max_length=64, unique=True)`
    - `newsletter/models.py` contains `unsubscribe_token = models.CharField(max_length=64, unique=True)`
    - `newsletter/models.py` contains `def generate_token()`
    - `newsletter/models.py` contains `def is_confirmation_expired(self)`
    - `newsletter/forms.py` contains `class NewsletterSignupForm(forms.Form):`
    - `newsletter/forms.py` contains `consent_newsletter = forms.BooleanField`
    - `newsletter/emails.py` contains `def send_confirmation_email(subscriber, request):`
    - `newsletter/emails.py` contains `"Potwierdz zapis do newslettera"`
    - `newsletter/admin.py` contains `@admin.register(Subscriber)`
    - `newsletter/admin.py` contains `class SubscriberAdmin(admin.ModelAdmin):`
    - `newsletter/urls.py` contains `app_name = "newsletter"`
    - `newsletter/urls.py` contains `name="subscribe"`
    - `newsletter/urls.py` contains `name="check_email"`
    - `newsletter/urls.py` contains `name="confirm"`
    - `newsletter/urls.py` contains `name="unsubscribe"`
    - `newsletter/views.py` contains `def subscribe(request):`
    - `newsletter/views.py` contains `def confirm(request, token):`
    - `newsletter/views.py` contains `def unsubscribe(request, token):`
    - `newsletter/views.py` contains `IntegrityError`
    - `backend/settings.py` contains `"newsletter"` in INSTALLED_APPS
    - `backend/urls.py` contains `include("newsletter.urls")`
    - `newsletter/migrations/0001_initial.py` exists
    - `newsletter/tests/test_models.py` contains `class` (at least one test class)
    - `newsletter/tests/test_views.py` contains `class` (at least one test class)
    - `newsletter/tests/test_emails.py` contains `class` (at least one test class)
    - `python3 manage.py test newsletter -v2` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    Newsletter app is fully scaffolded with model, forms, email helper, admin, URL routing, views with full logic, and comprehensive test suite. App is registered in settings and URLs. All tests pass. Migration created and applied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Newsletter signup section template and base.html integration</name>
  <files>
    templates/includes/_newsletter_signup.html,
    templates/base.html,
    static/css/main.css
  </files>
  <read_first>
    templates/base.html,
    templates/includes/_footer.html,
    static/css/main.css,
    newsletter/forms.py,
    newsletter/urls.py,
    .planning/phases/06-newsletter/06-CONTEXT.md
  </read_first>
  <action>
    1. Create `templates/includes/_newsletter_signup.html` (per D-01, D-02, D-03):
       - A `<section class="kk-newsletter-section">` with a `<div class="container">` inside
       - Heading: `<h3>` with text like "Zapisz sie do newslettera" using `font-family: var(--kk-font-heading)`
       - Subheading: Short text like "Otrzymuj przepisy i nowosci prosto na maila"
       - `<form method="POST" action="{% url 'newsletter:subscribe' %}">` with `{% csrf_token %}`
       - Email input field with Bootstrap `form-control` class, placeholder "Twoj adres email"
       - RODO consent checkbox: `<div class="form-check">` with `<input type="checkbox" name="consent_newsletter" class="form-check-input" required>` and `<label class="form-check-label">Wyrazam zgode na otrzymywanie newslettera. <a href="{% url 'privacy-policy' %}" target="_blank">Polityka prywatnosci</a></label>`
       - Submit button: `<button type="submit" class="btn kk-btn-primary">Zapisz sie</button>`
       - Layout: use Bootstrap `row` + `col-md-6 mx-auto` for centered, medium-width form
       - Text-center alignment for heading and subheading

    2. Modify `templates/base.html` to include newsletter section BETWEEN `</main>` and the footer include (per D-01):
       - Change from:
         ```
         </main>

         {% include "includes/_footer.html" %}
         ```
       - To:
         ```
         </main>

         {% include "includes/_newsletter_signup.html" %}

         {% include "includes/_footer.html" %}
         ```

    3. Add newsletter section CSS to `static/css/main.css`:
       - `.kk-newsletter-section`: `background-color: var(--kk-cream)`, `padding: 3rem 0`, `text-align: center`
       - `.kk-newsletter-section h3`: `font-family: var(--kk-font-heading)`, `color: var(--kk-olive-dark)`, `margin-bottom: 0.5rem`
       - `.kk-newsletter-section p`: `color: var(--kk-text-light)`, `margin-bottom: 1.5rem`
       - `.kk-newsletter-section .form-check-label`: `font-size: 0.875rem`
       - `.kk-newsletter-section .form-check-label a`: `color: var(--kk-olive)`, `text-decoration: underline`
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && python3 manage.py test -v2 && python3 -c "
from pathlib import Path
base = Path('templates/base.html').read_text()
assert '_newsletter_signup.html' in base, 'Newsletter include missing from base.html'
signup = Path('templates/includes/_newsletter_signup.html').read_text()
assert 'newsletter:subscribe' in signup, 'Form action URL missing'
assert 'csrf_token' in signup, 'CSRF token missing'
assert 'consent_newsletter' in signup, 'RODO checkbox missing'
css = Path('static/css/main.css').read_text()
assert 'kk-newsletter-section' in css, 'Newsletter CSS missing'
print('All template checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/includes/_newsletter_signup.html` exists and contains `newsletter:subscribe`
    - `templates/includes/_newsletter_signup.html` contains `{% csrf_token %}`
    - `templates/includes/_newsletter_signup.html` contains `consent_newsletter`
    - `templates/includes/_newsletter_signup.html` contains `polityka-prywatnosci` or `privacy-policy`
    - `templates/includes/_newsletter_signup.html` contains `kk-newsletter-section`
    - `templates/base.html` contains `_newsletter_signup.html` BEFORE `_footer.html`
    - `static/css/main.css` contains `.kk-newsletter-section`
    - `python3 manage.py test -v2` exits 0 (full suite still passes)
  </acceptance_criteria>
  <done>
    Newsletter signup section with email input, RODO consent checkbox, and submit button is visible on every page, positioned between main content and footer. Section is styled with brand CSS variables. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
1. `python3 manage.py test newsletter -v2` -- all newsletter tests pass
2. `python3 manage.py test -v2` -- full suite passes (no regressions)
3. `python3 manage.py runserver` and visit any page -- newsletter section visible above footer
4. Newsletter form has email field, RODO checkbox, and submit button
5. Form action points to `/newsletter/zapisz/`
</verification>

<success_criteria>
- Newsletter app registered and migrations applied
- Subscriber model with dual tokens, confirmation expiry, unsubscribe flag
- All views implemented: subscribe (POST-redirect-GET), check_email, confirm, unsubscribe
- Newsletter signup form visible on every page between content and footer
- Admin interface shows subscribers with filtering
- All tests pass (model, views, emails)
</success_criteria>

<output>
After completion, create `.planning/phases/06-newsletter/06-01-SUMMARY.md`
</output>
