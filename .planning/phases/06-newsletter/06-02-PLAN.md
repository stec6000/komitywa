---
phase: 06-newsletter
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - templates/newsletter/check_email.html
  - templates/newsletter/confirmed.html
  - templates/newsletter/unsubscribed.html
  - templates/newsletter/link_expired.html
autonomous: false
requirements:
  - NEWS-01
  - NEWS-02
  - NEWS-03

must_haves:
  truths:
    - "After subscribing, user sees 'sprawdz email' page with instructions"
    - "After clicking valid confirmation link, user sees 'zapis potwierdzony' page"
    - "After clicking expired confirmation link, user sees 'link wygasl' page with re-subscribe guidance"
    - "After clicking unsubscribe link, user sees 'wypisano' page"
    - "After clicking unsubscribe link again, user sees 'juz wypisany' message"
  artifacts:
    - path: "templates/newsletter/check_email.html"
      provides: "Check email confirmation page"
      contains: "sprawdz"
    - path: "templates/newsletter/confirmed.html"
      provides: "Subscription confirmed page"
      contains: "potwierdzony"
    - path: "templates/newsletter/unsubscribed.html"
      provides: "Unsubscribed page"
      contains: "wypisany"
    - path: "templates/newsletter/link_expired.html"
      provides: "Expired link page"
      contains: "wygasl"
  key_links:
    - from: "newsletter/views.py"
      to: "templates/newsletter/check_email.html"
      via: "render in check_email view"
      pattern: 'newsletter/check_email'
    - from: "newsletter/views.py"
      to: "templates/newsletter/confirmed.html"
      via: "render in confirm view"
      pattern: 'newsletter/confirmed'
    - from: "newsletter/views.py"
      to: "templates/newsletter/unsubscribed.html"
      via: "render in unsubscribe view"
      pattern: 'newsletter/unsubscribed'
---

<objective>
Create the page templates for all newsletter user-facing flows: check-email confirmation page, subscription confirmed page, unsubscribed page, and expired link page.

Purpose: Completes the user-facing newsletter experience with branded pages for every step of the subscribe/confirm/unsubscribe flow.
Output: Four page templates matching site brand, all reachable via newsletter URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-newsletter/06-CONTEXT.md
@.planning/phases/06-newsletter/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- views that render these templates -->

From newsletter/views.py (expected after Plan 01):
```python
def check_email(request):
    return render(request, "newsletter/check_email.html")

def confirm(request, token):
    # renders "newsletter/confirmed.html" on success
    # renders "newsletter/link_expired.html" on expired token

def unsubscribe(request, token):
    # renders "newsletter/unsubscribed.html"
    # context includes "already_unsubscribed" boolean for D-07
```

From templates/base.html:
```html
{% block title %}Kuchenna Komitywa{% endblock %}
{% block content %}{% endblock %}
```

CSS variables from static/css/main.css:
```css
--kk-sage: #9CAF88;
--kk-olive: #6B7F5E;
--kk-olive-dark: #4A5A40;
--kk-cream: #F5F0E8;
--kk-warm-white: #FDFBF7;
--kk-font-heading: "Lora", serif;
--kk-font-body: "Nunito", sans-serif;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Newsletter page templates (check-email, confirmed, unsubscribed, link-expired)</name>
  <files>
    templates/newsletter/check_email.html,
    templates/newsletter/confirmed.html,
    templates/newsletter/unsubscribed.html,
    templates/newsletter/link_expired.html
  </files>
  <read_first>
    newsletter/views.py,
    templates/base.html,
    templates/pages/kontakt.html,
    static/css/main.css,
    .planning/phases/06-newsletter/06-CONTEXT.md
  </read_first>
  <action>
    All four templates extend `base.html` and use the same layout pattern as existing pages (e.g., `templates/pages/kontakt.html`). Each page has a centered content area with heading, message, and a link back to the home page.

    1. Create `templates/newsletter/check_email.html` (per D-03):
       - `{% extends "base.html" %}`
       - `{% block title %}Sprawdz email -- Kuchenna Komitywa{% endblock %}`
       - `{% block content %}` with a `<section class="container py-5 text-center">`:
         - `<h1>` with `font-family: var(--kk-font-heading)`: "Sprawdz swoja skrzynke email"
         - `<p>`: "Wyslalismy link potwierdzajacy. Kliknij go, zeby dokonczyc zapis do newslettera."
         - `<p class="text-muted">`: "Link jest wazny przez 24 godziny."
         - `<a href="{% url 'home' %}" class="btn kk-btn-primary mt-3">Wroc do strony glownej</a>`

    2. Create `templates/newsletter/confirmed.html` (per D-04):
       - `{% extends "base.html" %}`
       - `{% block title %}Zapis potwierdzony -- Kuchenna Komitywa{% endblock %}`
       - `{% block content %}` with centered section:
         - `<h1>`: "Zapis potwierdzony!"
         - Bootstrap icon: `<i class="bi bi-check-circle" style="font-size: 3rem; color: var(--kk-sage);"></i>`
         - `<p>`: "Dziekujemy! Twoj zapis do newslettera Kuchennej Komitywy zostal potwierdzony."
         - `<a href="{% url 'home' %}">Wroc do strony glownej</a>`

    3. Create `templates/newsletter/unsubscribed.html` (per D-06, D-07):
       - `{% extends "base.html" %}`
       - `{% block title %}Wypisano z newslettera -- Kuchenna Komitywa{% endblock %}`
       - `{% block content %}` with centered section:
         - Use `{% if already_unsubscribed %}` conditional:
           - If true: `<h1>`: "Juz wypisany/a" and `<p>`: "Ten adres email zostal juz wczesniej wypisany z newslettera."
           - If false: `<h1>`: "Wypisano z newslettera" and `<p>`: "Zostales/as wypisany/a z newslettera Kuchennej Komitywy. Szkoda, ze odchodzisz!"
         - `<a href="{% url 'home' %}">Wroc do strony glownej</a>`

    4. Create `templates/newsletter/link_expired.html` (per Claude's Discretion -- expired token UX):
       - `{% extends "base.html" %}`
       - `{% block title %}Link wygasl -- Kuchenna Komitywa{% endblock %}`
       - `{% block content %}` with centered section:
         - `<h1>`: "Link wygasl"
         - `<p>`: "Niestety, link potwierdzajacy stracil waznosc. Sprobuj zapisac sie ponownie."
         - `<a href="{% url 'home' %}" class="btn kk-btn-primary mt-3">Wroc do strony glownej</a>`
         - (Home page has the newsletter form in the footer section, so user can re-subscribe from there)
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && python3 manage.py test newsletter -v2 && python3 -c "
from pathlib import Path
for name in ['check_email', 'confirmed', 'unsubscribed', 'link_expired']:
    p = Path(f'templates/newsletter/{name}.html')
    assert p.exists(), f'{p} missing'
    content = p.read_text()
    assert 'extends' in content, f'{p} does not extend base.html'
    assert 'block content' in content, f'{p} missing content block'
print('All newsletter templates verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/newsletter/check_email.html` exists and contains `{% extends "base.html" %}`
    - `templates/newsletter/check_email.html` contains "Sprawdz" (in heading)
    - `templates/newsletter/confirmed.html` exists and contains `{% extends "base.html" %}`
    - `templates/newsletter/confirmed.html` contains "potwierdzony"
    - `templates/newsletter/unsubscribed.html` exists and contains `{% extends "base.html" %}`
    - `templates/newsletter/unsubscribed.html` contains `already_unsubscribed`
    - `templates/newsletter/unsubscribed.html` contains "wypisany"
    - `templates/newsletter/link_expired.html` exists and contains `{% extends "base.html" %}`
    - `templates/newsletter/link_expired.html` contains "wygasl"
    - `python3 manage.py test newsletter -v2` exits 0
  </acceptance_criteria>
  <done>
    All four newsletter page templates exist, extend base.html, display correct Polish content for each flow state, and the unsubscribed template handles the already-unsubscribed case with a conditional.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete newsletter flow</name>
  <files>None (verification only)</files>
  <action>
    Human verifies the complete newsletter subscription system end-to-end.

    What was built: Complete newsletter subscription system with signup form on every page, double opt-in email confirmation, and one-click unsubscribe with all page templates.

    How to verify:
    1. Run `python3 manage.py runserver` and open http://127.0.0.1:8000/
    2. Scroll down -- verify newsletter signup section appears ABOVE the footer with email field, RODO checkbox, and "Zapisz sie" button
    3. Try submitting without checking the RODO checkbox -- browser should block submission
    4. Enter a test email, check the checkbox, submit -- you should be redirected to /newsletter/sprawdz-email/ page
    5. Check the terminal (console email backend) for the confirmation email -- it should contain a confirmation link and unsubscribe link
    6. Copy the confirmation link and paste in browser -- you should see "Zapis potwierdzony!" page
    7. Copy the unsubscribe link from the email and paste in browser -- you should see "Wypisano z newslettera" page
    8. Paste the same unsubscribe link again -- you should see "Juz wypisany/a" message
    9. Submit the form again with the same email -- should redirect to check-email page (privacy, no status revealed)
    10. Visit any other page (e.g., /przepisy/) and confirm newsletter section also appears there

    Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && python3 manage.py test -v2</automated>
  </verify>
  <done>Human confirms all 10 verification steps pass. Complete newsletter flow works: subscribe, check email, confirm, unsubscribe (including idempotent re-click).</done>
</task>

</tasks>

<verification>
1. `python3 manage.py test newsletter -v2` -- all newsletter tests pass
2. `python3 manage.py test -v2` -- full test suite passes
3. All four page templates render without errors
4. Newsletter signup form visible on every page
5. Complete flow works: subscribe -> check email -> confirm -> unsubscribe
</verification>

<success_criteria>
- All four newsletter page templates exist and extend base.html
- Check-email page shows confirmation instructions
- Confirmed page shows success message with green checkmark
- Unsubscribed page distinguishes between first and repeat unsubscribe (D-07)
- Link-expired page shows re-subscribe guidance
- Human verifies complete end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/06-newsletter/06-02-SUMMARY.md`
</output>
