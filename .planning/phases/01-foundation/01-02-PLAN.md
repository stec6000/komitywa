---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - templates/base.html
  - templates/includes/_navbar.html
  - templates/includes/_footer.html
  - templates/pages/home.html
  - static/css/main.css
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "Every page renders with shared header (navbar) and footer"
    - "Navbar shows brand name, nav links (Przepisy, Sklep, O nas, Kontakt), and cart icon"
    - "Pages display correctly on mobile (responsive Bootstrap 5 layout)"
    - "Brand colors (greens/beiges) and typography (Lora/Nunito) are applied"
    - "Static CSS file loads without 404"
  artifacts:
    - path: "templates/base.html"
      provides: "Master template with head, nav include, content block, footer include"
      contains: "{% block content %}"
    - path: "templates/includes/_navbar.html"
      provides: "Navigation partial with brand, links, cart icon"
      contains: "Przepisy"
    - path: "templates/includes/_footer.html"
      provides: "Footer partial with copyright"
      contains: "Kuchenna Komitywa"
    - path: "static/css/main.css"
      provides: "Brand colors, typography, custom styles"
      contains: "--kk-olive"
    - path: "templates/pages/home.html"
      provides: "Placeholder home page extending base.html"
      contains: "{% extends"
  key_links:
    - from: "templates/base.html"
      to: "templates/includes/_navbar.html"
      via: "{% include %}"
      pattern: "include.*_navbar"
    - from: "templates/base.html"
      to: "templates/includes/_footer.html"
      via: "{% include %}"
      pattern: "include.*_footer"
    - from: "templates/base.html"
      to: "static/css/main.css"
      via: "{% static %}"
      pattern: "static.*css/main.css"
    - from: "templates/pages/home.html"
      to: "templates/base.html"
      via: "{% extends %}"
      pattern: "extends.*base.html"
---

<objective>
Create the complete template hierarchy with base.html, navigation, footer, brand styling, and responsive Bootstrap 5 layout.

Purpose: This is the visual skeleton that every future page inherits. Phase 2 (landing), Phase 3 (recipes), Phase 4 (shop) all extend base.html. Getting the template hierarchy, brand colors, typography, and responsive layout right here means every future page automatically gets consistent styling.

Output: Working base.html with navbar, footer, Bootstrap 5 CDN, Google Fonts, brand CSS custom properties, and a placeholder home page that demonstrates the full template chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@backend/settings.py
@static/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base.html with Bootstrap 5, Google Fonts, navbar, and footer</name>
  <files>templates/base.html, templates/includes/_navbar.html, templates/includes/_footer.html</files>
  <read_first>backend/settings.py, .planning/phases/01-foundation/01-UI-SPEC.md, .planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
Create the template directory structure: `templates/`, `templates/includes/`, `templates/pages/`.

**templates/base.html** -- Master template. Must include:
- `<!DOCTYPE html>` with `<html lang="pl">`
- `<meta charset="utf-8">` and `<meta name="viewport" content="width=device-width, initial-scale=1">`
- `<title>{% block title %}Kuchenna Komitywa{% endblock %}</title>`
- `{% load static %}` at top
- Preconnect to `fonts.googleapis.com` and `fonts.gstatic.com`
- Google Fonts link loading Lora (weights 400, 700) and Nunito (weights 400, 700) with `display=swap`
- Bootstrap 5.3 CSS CDN link: `https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css` (use 5.3.3 which is the latest stable with known integrity hash; fetch the actual integrity hash from CDN at implementation time)
- Bootstrap Icons CDN link: `https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css`
- `<link rel="stylesheet" href="{% static 'css/main.css' %}">`
- `{% block extra_css %}{% endblock %}`
- Skip-to-content link: `<a href="#main-content" class="skip-link">Przejdz do tresci</a>` (hidden by default, visible on focus)
- `{% include "includes/_navbar.html" %}`
- `<main id="main-content">{% block content %}{% endblock %}</main>`
- `{% include "includes/_footer.html" %}`
- Bootstrap 5.3 JS bundle CDN: `https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js`
- `{% block extra_js %}{% endblock %}`

**templates/includes/_navbar.html** -- Per UI-SPEC Component 1:
- `{% load static %}` at top
- `<nav class="navbar navbar-expand-lg">` with inline style or CSS class for background --kk-cream
- Container with brand link: `<a class="navbar-brand" href="{% url 'home' %}">Kuchenna Komitywa</a>` styled in Lora 20px bold, color --kk-olive-dark
- Hamburger toggle button with `data-bs-toggle="collapse"` and `data-bs-target="#mainNav"`
- Collapse div `#mainNav` containing:
  - `<ul class="navbar-nav ms-auto">` with 4 nav items: Przepisy, Sklep, O nas, Kontakt (per D-01)
  - All nav links use `href="#"` as placeholder (real URLs come in future phases)
  - Cart icon link after nav items: `<a class="nav-link position-relative ms-3" href="#" aria-label="Koszyk"><i class="bi bi-cart3"></i><span class="position-absolute top-0 start-100 translate-middle badge rounded-pill kk-badge">0</span></a>` (per D-03)
- Bottom border: 1px solid --kk-beige

**templates/includes/_footer.html** -- Per UI-SPEC Component 2:
- `<footer>` element with class for cream background
- Padding: 48px top, 24px bottom (matching UI-SPEC)
- Top border: 1px solid --kk-beige
- Centered text: "(C) 2026 Kuchenna Komitywa. Wszelkie prawa zastrzezone." in Nunito 14px, color --kk-text-light
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && python -c "
from pathlib import Path
base = Path('templates/base.html')
nav = Path('templates/includes/_navbar.html')
foot = Path('templates/includes/_footer.html')
assert base.exists(), 'base.html missing'
assert nav.exists(), '_navbar.html missing'
assert foot.exists(), '_footer.html missing'
b = base.read_text()
assert '{% block content %}' in b, 'missing content block'
assert '_navbar.html' in b, 'missing navbar include'
assert '_footer.html' in b, 'missing footer include'
assert 'bootstrap' in b.lower(), 'missing bootstrap'
assert 'Lora' in b, 'missing Lora font'
assert 'Nunito' in b, 'missing Nunito font'
n = nav.read_text()
assert 'Przepisy' in n, 'missing nav link Przepisy'
assert 'Sklep' in n, 'missing nav link Sklep'
assert 'O nas' in n, 'missing nav link O nas'
assert 'Kontakt' in n, 'missing nav link Kontakt'
assert 'bi-cart3' in n, 'missing cart icon'
assert 'aria-label' in n, 'missing cart aria-label'
f = foot.read_text()
assert 'Kuchenna Komitywa' in f, 'missing footer brand'
print('All template checks PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - templates/base.html contains `<html lang="pl">`
    - templates/base.html contains `{% load static %}`
    - templates/base.html contains `{% block title %}Kuchenna Komitywa{% endblock %}`
    - templates/base.html contains `{% block content %}{% endblock %}`
    - templates/base.html contains `{% include "includes/_navbar.html" %}`
    - templates/base.html contains `{% include "includes/_footer.html" %}`
    - templates/base.html contains `bootstrap` CSS CDN link
    - templates/base.html contains `bootstrap-icons` CDN link
    - templates/base.html contains `Lora` and `Nunito` Google Fonts references
    - templates/base.html contains `{% static 'css/main.css' %}`
    - templates/base.html contains `<a href="#main-content"` (skip link)
    - templates/base.html contains `<main id="main-content">`
    - templates/includes/_navbar.html contains `Przepisy`, `Sklep`, `O nas`, `Kontakt`
    - templates/includes/_navbar.html contains `bi-cart3`
    - templates/includes/_navbar.html contains `aria-label="Koszyk"`
    - templates/includes/_navbar.html contains `{% url 'home' %}`
    - templates/includes/_footer.html contains `Kuchenna Komitywa`
    - templates/includes/_footer.html contains `<footer`
  </acceptance_criteria>
  <done>base.html renders with Bootstrap 5, Google Fonts, navbar with all 4 nav links + cart icon, and footer with copyright</done>
</task>

<task type="auto">
  <name>Task 2: Create brand CSS and update home page to extend base.html</name>
  <files>static/css/main.css, templates/pages/home.html, core/tests.py</files>
  <read_first>templates/base.html, templates/includes/_navbar.html, templates/includes/_footer.html, .planning/phases/01-foundation/01-UI-SPEC.md, core/tests.py</read_first>
  <action>
**static/css/main.css** -- Brand design system per UI-SPEC Color and Typography sections:
```css
/* ===========================
   Kuchenna Komitywa - Brand CSS
   =========================== */

:root {
    /* Brand palette: warm greens + beiges (per D-09) */
    --kk-sage: #9CAF88;
    --kk-olive: #6B7F5E;
    --kk-olive-dark: #4A5A40;
    --kk-cream: #F5F0E8;
    --kk-warm-white: #FDFBF7;
    --kk-beige: #D4C5A9;
    --kk-earth: #8B7355;
    --kk-text: #3D3D3D;
    --kk-text-light: #6B6B6B;

    /* Typography (per D-10) */
    --kk-font-heading: "Lora", serif;
    --kk-font-body: "Nunito", sans-serif;
}

/* Base styles */
body {
    font-family: var(--kk-font-body);
    color: var(--kk-text);
    background-color: var(--kk-warm-white);
    font-size: 16px;
    line-height: 1.5;
}

h1, h2 {
    font-family: var(--kk-font-heading);
    color: var(--kk-olive-dark);
    font-size: 28px;
    font-weight: 700;
    line-height: 1.2;
}

h3, h4, h5, h6 {
    font-family: var(--kk-font-heading);
    color: var(--kk-olive-dark);
    font-size: 20px;
    font-weight: 700;
    line-height: 1.2;
}

/* Skip-to-content link */
.skip-link {
    position: absolute;
    top: -40px;
    left: 0;
    background: var(--kk-olive-dark);
    color: white;
    padding: 8px 16px;
    z-index: 1100;
    transition: top 0.2s;
}

.skip-link:focus {
    top: 0;
}

/* Navbar styles (per UI-SPEC Component 1) */
.navbar {
    background-color: var(--kk-cream);
    border-bottom: 1px solid var(--kk-beige);
    min-height: 56px;
}

.navbar-brand {
    font-family: var(--kk-font-heading);
    font-size: 20px;
    font-weight: 700;
    color: var(--kk-olive-dark) !important;
}

.navbar .nav-link {
    font-family: var(--kk-font-body);
    font-size: 16px;
    font-weight: 400;
    color: var(--kk-text);
    transition: color 200ms ease;
}

.navbar .nav-link:hover {
    color: var(--kk-olive);
}

.navbar .nav-link.active {
    color: var(--kk-olive);
    font-weight: 700;
}

.navbar .nav-link:focus-visible {
    outline: 2px solid var(--kk-olive);
    outline-offset: 2px;
}

/* Cart badge */
.kk-badge {
    background-color: var(--kk-olive) !important;
    color: white;
    font-size: 12px;
}

.kk-badge-dim {
    opacity: 0.6;
}

/* Footer styles (per UI-SPEC Component 2) */
.kk-footer {
    background-color: var(--kk-cream);
    border-top: 1px solid var(--kk-beige);
    padding: 48px 0 24px 0;
    color: var(--kk-text-light);
    font-size: 14px;
    text-align: center;
}

/* Placeholder home page centering */
.kk-placeholder {
    min-height: calc(100vh - 56px - 96px);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.kk-placeholder h1 {
    font-family: var(--kk-font-heading);
    font-size: 28px;
    font-weight: 700;
    color: var(--kk-olive-dark);
}

.kk-placeholder p {
    font-size: 16px;
    color: var(--kk-text-light);
}
```

**templates/pages/home.html** -- Replace the temporary placeholder from Plan 01 with a version extending base.html (per UI-SPEC Component 4):
```html
{% extends "base.html" %}

{% block title %}Kuchenna Komitywa{% endblock %}

{% block content %}
<div class="container kk-placeholder">
    <h1>Kuchenna Komitywa</h1>
    <p>Strona w budowie</p>
</div>
{% endblock %}
```

**core/tests.py** -- Add template integration tests (append to existing tests):
```python
class TestBaseTemplate(TestCase):
    """FOUND-02: base.html renders with nav, footer."""

    def test_home_page_has_navbar(self):
        response = self.client.get("/")
        self.assertContains(response, "Przepisy")
        self.assertContains(response, "Sklep")
        self.assertContains(response, "O nas")
        self.assertContains(response, "Kontakt")

    def test_home_page_has_cart_icon(self):
        response = self.client.get("/")
        self.assertContains(response, "bi-cart3")
        self.assertContains(response, 'aria-label="Koszyk"')

    def test_home_page_has_footer(self):
        response = self.client.get("/")
        self.assertContains(response, "Kuchenna Komitywa")
        self.assertContains(response, "Wszelkie prawa")

    def test_home_page_has_skip_link(self):
        response = self.client.get("/")
        self.assertContains(response, "Przejdz do tresci")


class TestResponsiveLayout(TestCase):
    """FOUND-03: Pages are responsive with Bootstrap 5."""

    def test_bootstrap_css_included(self):
        response = self.client.get("/")
        self.assertContains(response, "bootstrap")

    def test_viewport_meta_tag(self):
        response = self.client.get("/")
        self.assertContains(response, "viewport")

    def test_static_css_included(self):
        response = self.client.get("/")
        self.assertContains(response, "main.css")
```
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && python manage.py test core.tests -v2 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - static/css/main.css contains `--kk-sage: #9CAF88`
    - static/css/main.css contains `--kk-olive: #6B7F5E`
    - static/css/main.css contains `--kk-olive-dark: #4A5A40`
    - static/css/main.css contains `--kk-cream: #F5F0E8`
    - static/css/main.css contains `--kk-warm-white: #FDFBF7`
    - static/css/main.css contains `--kk-font-heading: "Lora", serif`
    - static/css/main.css contains `--kk-font-body: "Nunito", sans-serif`
    - static/css/main.css contains `.kk-footer`
    - static/css/main.css contains `.skip-link`
    - templates/pages/home.html contains `{% extends "base.html" %}`
    - templates/pages/home.html contains `{% block content %}`
    - templates/pages/home.html contains `Strona w budowie`
    - core/tests.py contains `class TestBaseTemplate`
    - core/tests.py contains `class TestResponsiveLayout`
    - `python manage.py test core.tests -v2` exits 0
  </acceptance_criteria>
  <done>Brand CSS with all color variables and typography loaded, home page extends base.html, navbar and footer render on every page, Bootstrap 5 responsive layout works, all tests pass</done>
</task>

</tasks>

<verification>
- `python manage.py test core.tests -v2` -- all tests pass (including TestBaseTemplate, TestResponsiveLayout)
- GET / returns 200 with navbar containing all 4 nav links
- GET / response includes Bootstrap CSS CDN link
- GET / response includes main.css static reference
- GET / response includes footer with copyright
- Page renders responsively (Bootstrap grid present)
</verification>

<success_criteria>
- base.html exists and is used by home page via {% extends %}
- Navbar shows: brand link, Przepisy, Sklep, O nas, Kontakt, cart icon with badge
- Footer shows copyright text
- Bootstrap 5 CSS and JS loaded via CDN
- Google Fonts (Lora + Nunito) loaded via CDN
- Brand CSS custom properties defined (all 9 colors + 2 font families)
- Skip-to-content accessibility link present
- All responsive, typography, and template tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
