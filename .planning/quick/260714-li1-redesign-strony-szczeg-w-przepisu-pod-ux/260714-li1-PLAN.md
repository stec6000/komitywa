---
phase: quick-260714-li1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recipes/templatetags/__init__.py
  - recipes/templatetags/recipe_extras.py
  - templates/recipes/detail.html
  - static/css/main.css
autonomous: false
requirements: [LI1-UX-DETAIL]
must_haves:
  truths:
    - "Składniki są w prawej kolumnie i przyklejają się (sticky) podczas scrollowania kroków na desktopie"
    - "Na górze pod tytułem jest jeden ładny meta-bar z czasem, porcjami i trudnością (bez dublowania w osobnym sidebarze)"
    - "Stary sidebar 'notatnik' zniknął — nie ma podwójnej prezentacji tych samych meta danych"
    - "Nagłówki grup składników (linie kończące się ':') są wyróżnione stylem"
    - "Na mobile składniki są w DOM PRZED krokami i meta-bar zawija się czytelnie"
    - "Sekcja 'Od autora' (notes) działa jak wcześniej, bez parsowania kcal"
  artifacts:
    - path: "recipes/templatetags/recipe_extras.py"
      provides: "Filtr format_ingredients wyróżniający nagłówki grup składników"
      contains: "def format_ingredients"
    - path: "templates/recipes/detail.html"
      provides: "Przebudowany layout: meta-bar + dwukolumnowy body ze sticky składnikami"
      contains: "kk-recipe-body"
    - path: "static/css/main.css"
      provides: "Style meta-bar, sticky ingredients column, mobile ordering, group headers"
      contains: "kk-meta-bar"
  key_links:
    - from: "templates/recipes/detail.html"
      to: "recipes/templatetags/recipe_extras.py"
      via: "{% load recipe_extras %} + |format_ingredients"
      pattern: "format_ingredients"
    - from: ".kk-recipe-body (mobile)"
      to: "DOM order: składniki przed krokami"
      via: "grid single-column default + order swap tylko w @media min-width:992px"
      pattern: "order:"
---

<objective>
Redesign strony szczegółów przepisu (/przepisy/<slug>/) pod UX: składniki przenieść do prawej sticky kolumny, meta dane (czas/porcje/trudność) dać w jednym ładnym meta-barze pod tytułem, usunąć redundantny sidebar „notatnik".

Purpose: Obecny layout jest nieczytelny — składniki spychają kroki w dół, a sticky sidebar dubluje badge'e pod tytułem. Chcemy: kroki od razu widoczne z zawsze-widocznymi składnikami obok.
Output: Zmodyfikowany template + CSS + drobny templatetag do wyróżniania nagłówków grup składników.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Recipe model (recipes/models.py) — pola dostępne w template `recipe`: -->
<!-- title, slug, category (FK, .name/.slug, nullable), tags (M2M, .name/.slug),
     description, ingredients_text (TextField, linie; grupy kończą się ':'),
     steps_text (TextField, linie), prep_time (int min), servings (int),
     difficulty (choices: latwy/sredni/trudny), notes (TextField, może zawierać kcal — NIE parsować),
     image (nullable), created_at -->

<!-- Grid system (static/css/main.css ~1598): własny 12-col CSS grid, .row = display:grid.
     Poniżej 992px col-lg-* => span 12 (stackują się w kolejności DOM). -->
<!-- Nav (static/css/main.css ~254): position: sticky; top:0; z-index:50 — sticky offset składników musi to uwzględnić (~100px). -->
<!-- CSS vars: --font-display (DM Serif), --font-hand (Caveat), --font-body (Newsreader),
     --ink, --ink-soft, --ink-faded, --paper, --paper-2, --paper-shadow, --accent(olive), --accent-2(terracotta), --accent-3(mustard), --rule, --page-fold. -->
<!-- Istniejące klasy do reużycia/stylistyki: .kk-detail-hero, .kk-ingredients, .kk-steps, .kk-notes,
     .kk-prep-time (rotowany badge), .kk-category-badge, .hero-eyebrow, .tag-row/.tag, .kk-link-arrow, .featured-label. -->
<!-- Stary sidebar do USUNIĘCIA: .kk-detail-sidebar (main.css ~1360, oraz reguła static w media ~1658). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Templatetag filter dla nagłówków grup składników</name>
  <files>recipes/templatetags/__init__.py, recipes/templatetags/recipe_extras.py</files>
  <action>
    Utwórz pakiet templatetags dla aplikacji recipes (pusty `__init__.py`).
    W `recipe_extras.py` zdefiniuj `register = template.Library()` i filtr `format_ingredients(text)`, który:
    - zwraca "" dla pustego/None wejścia;
    - dzieli tekst po liniach, pomija puste (strip);
    - buduje `<ul class="kk-ingredient-list">` gdzie każda linia to `<li>`;
    - linie kończące się ":" oznacza klasą `kk-ingredient-group` (nagłówek grupy, np. "Sos:"), pozostałe klasą `kk-ingredient-item`;
    - KAŻDĄ linię przepuszcza przez `django.utils.html.escape` PRZED wstawieniem do HTML (ochrona XSS — treść pochodzi z modelu edytowalnego w adminie), całość owija `mark_safe`.
    Używaj podwójnych cudzysłowów (konwencja projektu). Nie ruszaj kcal / notes — filtr dotyczy wyłącznie ingredients_text.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','backend.settings');django.setup();from recipes.templatetags.recipe_extras import format_ingredients;o=format_ingredients('Sos:\naa\n<b>x</b>');assert 'kk-ingredient-group' in o and 'kk-ingredient-item' in o and '&lt;b&gt;' in o,o;print('OK')"</automated>
  </verify>
  <done>Filtr zwraca ul z wyróżnionymi grupami (endswith ':') i zescapowanymi itemami; nagłówki i itemy mają osobne klasy CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Przebudowa detail.html + style (meta-bar, sticky składniki, mobile order)</name>
  <files>templates/recipes/detail.html, static/css/main.css</files>
  <action>
    TEMPLATE (templates/recipes/detail.html):
    - Dodaj `{% load recipe_extras %}` obok `{% load static %}`.
    - NIE ruszaj bloku `extra_js` ze `schema_json` / JSON-LD.
    - Zastąp obecny układ `.row > col-lg-8 / col-lg-4` nową strukturą wewnątrz `.wrap`:
      1) NAGŁÓWEK (pełna szerokość): hero image (jeśli jest, klasa .kk-detail-hero bez zmian), `hero-eyebrow` z kategorią, `<h1>`.
      2) META-BAR pod h1: kontener `.kk-meta-bar` z pozycjami `.kk-meta-item` (label `.kk-meta-label` + wartość `.kk-meta-value`) dla: czas ({{ recipe.prep_time }} min), porcje ({{ recipe.servings }} + poprawna polska odmiana jak obecnie: 1→porcja, <5→porcje, else→porcji), trudność ({{ recipe.difficulty }}). Opcjonalnie kategoria jako pozycja meta-bar lub zostaw eyebrow — Twoja dyskrecja, byle bez dublowania.
      3) `<p class="lead">{{ recipe.description }}</p>`, pod nią (jeśli są tagi) `.tag-row` z linkami do `?tag=` (przenieś z usuwanego sidebaru).
      4) BODY dwukolumnowy `.kk-recipe-body`. WAŻNE: w DOM NAJPIERW składniki, POTEM kroki (dla mobile order):
         - `<aside class="kk-recipe-ingredients-col">` zawiera `.kk-ingredients-sticky` z `<h2>Składniki</h2>`, `<div class="kk-ingredients kk-ingredients--list">{{ recipe.ingredients_text|format_ingredients }}</div>` oraz link `.kk-link-arrow` „← wróć do przepiśnika" ({% url 'recipes:list' %}) pod składnikami.
         - `<div class="kk-recipe-steps-col">` zawiera `<h2>Sposób przygotowania</h2>`, `.kk-steps` (steps_text|linebreaksbr), oraz jeśli notes: `<h2>Od autora</h2>` + `.kk-notes` (bez zmian, bez parsowania kcal).
    - USUŃ całą starą `<aside class="kk-detail-sidebar">` (notatnik) — jej dane żyją teraz w meta-barze / linku powrotnym.

    CSS (static/css/main.css, sekcja „Detail / sidebar" ~1347):
    - Dodaj `.kk-meta-bar`: notatnikowy pasek spójny z estetyką (border 1.5px var(--ink), tło var(--paper-2)/var(--paper), box-shadow jak inne elementy, ewentualnie lekki rotate na itemach jak .kk-prep-time), display:flex; flex-wrap:wrap; gap. `.kk-meta-item` kolumnowo label+value; `.kk-meta-label` małe, `--font-hand` lub eyebrow, uppercase/kolor --ink-faded; `.kk-meta-value` `--font-display`, większe. Dopasuj do palety, nie inline-style.
    - `.kk-recipe-body`: domyślnie (mobile-first) `display:grid; grid-template-columns:1fr; gap`. Dzięki temu składniki (pierwsze w DOM) są NAD krokami na mobile.
    - `.kk-ingredients-sticky`: `position: sticky; top: 100px;` (uwzględnia sticky nav top:0), `max-height: calc(100vh - 130px); overflow-y:auto;` dla bardzo długich list.
    - `.kk-ingredients--list ul.kk-ingredient-list`: reset list-style, padding; `.kk-ingredient-item` z subtelnym myślnikiem/markerem; `.kk-ingredient-group` wyróżniony (bold / eyebrow / `--font-hand`, kolor --accent-2 lub --ink, margin-top większy, pierwszy bez marginu).
    - W `@media (min-width: 992px)`: `.kk-recipe-body { grid-template-columns: minmax(0,1fr) 340px; }` oraz `.kk-recipe-steps-col{order:1}` `.kk-recipe-ingredients-col{order:2}` — kroki po lewej, sticky składniki po prawej (order swap TYLKO tu, żeby mobile DOM order został zachowany).
    - USUŃ regułę `.kk-detail-sidebar { position: static; }` z media (~1658) oraz — jeśli nie używana przez inne szablony — blok `.kk-detail-sidebar` (~1360). Sprawdź `grep -rn "kk-detail-sidebar" templates/` przed usunięciem definicji; jeśli używana gdzie indziej, zostaw definicję a usuń tylko z detail.html.
    - Meta-bar musi ładnie zawijać się na wąskich ekranach (flex-wrap już to daje; dopilnuj gap/padding w media ≤600px jeśli trzeba).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','backend.settings');django.setup();from django.template.loader import get_template;t=get_template('recipes/detail.html');print('template OK')" && grep -q "kk-recipe-body" templates/recipes/detail.html && grep -q "format_ingredients" templates/recipes/detail.html && ! grep -q "kk-detail-sidebar" templates/recipes/detail.html && grep -q "kk-meta-bar" static/css/main.css && echo ALL_OK</automated>
  </verify>
  <done>detail.html renderuje się bez błędów; ma meta-bar, dwukolumnowy body ze sticky składnikami, składniki przed krokami w DOM; stary sidebar 'notatnik' usunięty; JSON-LD nietknięty.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Przebudowany widok /przepisy/<slug>/: meta-bar pod tytułem, sticky składniki po prawej, kroki po lewej, brak sidebaru 'notatnik', wyróżnione nagłówki grup składników.</what-built>
  <how-to-verify>
    1. Uruchom serwer: `.venv/bin/python manage.py runserver` (w tle).
    2. Zrób screenshot desktop:
       `google-chrome --headless --disable-gpu --hide-scrollbars --window-size=1400,2200 --screenshot=/tmp/recipe-desktop.png http://127.0.0.1:8000/przepisy/weganskie-ragu-z-tofu/`
    3. Zrób screenshot mobile:
       `google-chrome --headless --disable-gpu --hide-scrollbars --window-size=390,2600 --screenshot=/tmp/recipe-mobile.png http://127.0.0.1:8000/przepisy/weganskie-ragu-z-tofu/`
    4. Otwórz oba PNG i sprawdź:
       - Desktop: meta-bar (czas/porcje/trudność) czytelny pod tytułem, spójny z marką; składniki po prawej, kroki po lewej; nagłówki grup składników (np. "Tofu mielone:", "Sos:") wyróżnione; brak drugiego, dublującego panelu meta danych.
       - Mobile: składniki POJAWIAJĄ SIĘ PRZED krokami; meta-bar zawija się ładnie; nic się nie rozjeżdża.
       - (Ręcznie w przeglądarce) scroll: składniki trzymają się na górze podczas przewijania kroków; bardzo długa lista dostaje własny scroll (max-height).
    5. Zamknij runserver.
  </how-to-verify>
  <resume-signal>Napisz "approved" albo opisz co poprawić (np. offset sticky, styl meta-baru, odstępy).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin content → rendered HTML | ingredients_text edytowalny w Django adminie; renderowany przez własny filtr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-li1-01 | Tampering/XSS | format_ingredients w recipe_extras.py | mitigate | Każda linia przez django.utils.html.escape przed mark_safe; brak surowego wstrzykiwania treści |
| T-li1-02 | Info disclosure | schema_json / JSON-LD | accept | Nie ruszamy JSON-LD; brak nowych danych eksponowanych |
</threat_model>

<verification>
- `format_ingredients` escapuje HTML i rozróżnia grupy/itemy (Task 1 automated).
- detail.html renderuje się i ma nowy layout, stary sidebar usunięty (Task 2 automated).
- Wizualna weryfikacja desktop+mobile przez headless chrome (Task 3 checkpoint).
</verification>

<success_criteria>
- Składniki w prawej sticky kolumnie na desktop; kroki od razu widoczne obok.
- Meta-bar z czasem/porcjami/trudnością pod tytułem, spójny z estetyką marki, bez dublowania.
- Sidebar „notatnik" usunięty (template + osierocone CSS).
- Nagłówki grup składników wyróżnione stylem.
- Mobile: składniki przed krokami w DOM, meta-bar zawija się czytelnie.
- Sekcja „Od autora" bez zmian, JSON-LD nietknięty.
</success_criteria>

<output>
Create `.planning/quick/260714-li1-redesign-strony-szczeg-w-przepisu-pod-ux/260714-li1-SUMMARY.md` when done
</output>
