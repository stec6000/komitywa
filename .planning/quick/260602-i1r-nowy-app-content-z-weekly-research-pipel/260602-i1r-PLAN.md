---
phase: 260602-i1r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/__init__.py
  - content/apps.py
  - content/models.py
  - content/admin.py
  - content/migrations/__init__.py
  - content/migrations/0001_initial.py
  - content/management/__init__.py
  - content/management/commands/__init__.py
  - content/management/commands/run_weekly_research.py
  - requirements.txt
  - backend/settings.py
autonomous: true
requirements:
  - QUICK-260602-i1r
must_haves:
  truths:
    - "Django app `content` is registered in INSTALLED_APPS and imports cleanly"
    - "Admin lists WeeklyResearch with status filter and shows raw_research/formatted_json as readonly"
    - "Management command `run_weekly_research` is registered (visible via manage.py --help) and exposes `--force`"
    - "Running the command without ANTHROPIC_API_KEY produces a CommandError (no silent failure)"
    - "RESEARCH_PROMPT and FORMAT_PROMPT in the command file match PROMPTS-VERBATIM.md character-for-character"
    - "ANTHROPIC_API_KEY is read from django.conf.settings (env-driven, empty default)"
    - "`python manage.py check` reports no issues after the migration is applied"
  artifacts:
    - path: "content/apps.py"
      provides: "ContentConfig AppConfig with name='content'"
      contains: "class ContentConfig"
    - path: "content/models.py"
      provides: "WeeklyResearch model with week_label/date_from/date_to/raw_research/formatted_json/status/error_message/timestamps"
      contains: "class WeeklyResearch"
    - path: "content/admin.py"
      provides: "WeeklyResearch admin registration with list_display, list_filter, search_fields, readonly_fields"
      contains: "@admin.register(WeeklyResearch)"
    - path: "content/migrations/0001_initial.py"
      provides: "Initial migration for WeeklyResearch"
      contains: "WeeklyResearch"
    - path: "content/management/commands/run_weekly_research.py"
      provides: "BaseCommand subclass with --force, idempotency, two-call Anthropic pipeline"
      contains: "class Command"
    - path: "requirements.txt"
      provides: "anthropic dependency entry"
      contains: "anthropic"
    - path: "backend/settings.py"
      provides: "'content' in INSTALLED_APPS and ANTHROPIC_API_KEY setting"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "backend/settings.py"
      to: "content app"
      via: "INSTALLED_APPS entry"
      pattern: "\"content\""
    - from: "content/management/commands/run_weekly_research.py"
      to: "content.models.WeeklyResearch"
      via: "ORM get_or_create / save"
      pattern: "WeeklyResearch\\.objects"
    - from: "content/management/commands/run_weekly_research.py"
      to: "settings.ANTHROPIC_API_KEY"
      via: "from django.conf import settings"
      pattern: "settings\\.ANTHROPIC_API_KEY"
    - from: "content/admin.py"
      to: "content.models.WeeklyResearch"
      via: "@admin.register"
      pattern: "@admin\\.register\\(WeeklyResearch\\)"
---

<objective>
Stworz nowy Django app `content/` ktory hostuje tygodniowy pipeline researchu + generowania
contentu dla marki "Kuchenna Komitywa". Pipeline sklada sie z modelu `WeeklyResearch`,
adminowej rejestracji oraz management command `run_weekly_research` ktory wykonuje
dwa wywolania Anthropic (research z web search + format do JSON-a).

Purpose: Domknac infrastrukture wewnetrznego narzedzia redakcyjnego — model + komenda
sa fundamentem przyszlej automatyzacji (cron) i panelu adminowego do podgladu wynikow.
Output: Nowy app `content/` z migracja, adminem i komenda gotowa do uruchomienia
po dostarczeniu ANTHROPIC_API_KEY przez `.env`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/quick/260602-i1r-nowy-app-content-z-weekly-research-pipel/PROMPTS-VERBATIM.md
@CLAUDE.md
@backend/settings.py
@backend/urls.py
@accounts/admin.py
@recipes/models.py
@requirements.txt
@static/css/main.css

<interfaces>
<!-- Konwencje projektu, do uwzglednienia w nowym kodzie -->

Konwencje stylu (z CLAUDE.md):
- Indentacja 4 spacje, double quotes dla wszystkich literalow stringowych
- snake_case dla modulow, PascalCase dla klas, snake_case dla funkcji
- 2 puste linie miedzy top-level definicjami, 1 pusta linia miedzy metodami klasy
- AppConfig per app (django-style)
- @admin.register(Model) decorator (z accounts/admin.py)
- DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField" (z settings.py)
- Implement __str__ na kazdym modelu

Lokalne apps w INSTALLED_APPS (kolejnosc z backend/settings.py):
    "accounts",
    "core",
    "recipes",
    "shop",
    "newsletter",
    # tu dodaj "content"

Paleta marki (z static/css/main.css linie 10-21):
    --paper:    #f3ead7
    --ink:      #2a2420
    --accent:   #6b7a3a   (olive)
    --accent-2: #b6562e   (terracotta)
    --accent-3: #c89a3a   (mustard)
(Te 5 kolorow JEST juz w FORMAT_PROMPT w PROMPTS-VERBATIM.md — nie podmieniaj nic.)

Recipe model jako wzor stylu (recipes/models.py):
- Meta z verbose_name / verbose_name_plural / ordering
- choices jako lista krotek
- help_text przy polach w jezyku polskim (bez polskich znakow dla zgodnosci z plikiem)

Anthropic Messages API (do referencji w komendzie):
- model = "claude-sonnet-4-6"
- web search tool: {"type": "web_search_20250305", "name": "web_search"}
- response.content to lista blokow; tylko bloki z .type == "text" maja .text
- bloki typu "tool_use" / "web_search_tool_result" / "server_tool_use" pomijamy
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Szkielet appa `content/` (AppConfig + puste moduly)</name>
  <files>
    content/__init__.py,
    content/apps.py,
    content/migrations/__init__.py,
    content/management/__init__.py,
    content/management/commands/__init__.py
  </files>
  <action>
    Utworz katalog `content/` z minimalnym szkieletem Django app:
    - `content/__init__.py` — pusty plik
    - `content/apps.py` — klasa `ContentConfig(AppConfig)` z `default_auto_field = "django.db.models.BigAutoField"`
      i `name = "content"`. Bez `verbose_name` (nie jest potrzebny).
    - `content/migrations/__init__.py` — pusty
    - `content/management/__init__.py` — pusty
    - `content/management/commands/__init__.py` — pusty

    NIE dodawaj jeszcze `content` do `INSTALLED_APPS` — to robi Task 5 (po dodaniu modelu i komendy,
    zeby `manage.py check` od razu przeszedl). Pliki `models.py`, `admin.py` i komenda pojawia sie
    w kolejnych taskach.
  </action>
  <verify>
    <automated>test -f content/__init__.py && test -f content/apps.py && test -f content/migrations/__init__.py && test -f content/management/__init__.py && test -f content/management/commands/__init__.py && grep -q "class ContentConfig" content/apps.py && grep -q "name = \"content\"" content/apps.py</automated>
  </verify>
  <done>
    Katalog `content/` istnieje z 5 plikami; `ContentConfig` zdefiniowany; brak importow modeli
    (jeszcze nie istnieja); brak wpisu w `INSTALLED_APPS`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Model `WeeklyResearch`</name>
  <files>content/models.py</files>
  <action>
    Utworz `content/models.py` z modelem `WeeklyResearch`:
    - `week_label = models.CharField(max_length=16, unique=True, db_index=True, help_text="Format ISO YYYY-Www, np. 2026-W23")`
    - `date_from = models.DateField()`
    - `date_to = models.DateField()`
    - `raw_research = models.TextField(blank=True, default="")`
    - `formatted_json = models.JSONField(null=True, blank=True)`
    - `status` — CharField max 20 z choices `[("pending","pending"), ("research_done","research_done"), ("formatted","formatted"), ("failed","failed")]`,
      `default="pending"`
    - `error_message = models.TextField(blank=True, default="")`
    - `created_at = models.DateTimeField(auto_now_add=True)`
    - `updated_at = models.DateTimeField(auto_now=True)`

    `Meta`:
      `verbose_name = "Weekly research"`
      `verbose_name_plural = "Weekly researches"`
      `ordering = ["-date_to"]`

    `__str__` zwraca `f"{self.week_label} ({self.status})"`.

    Styl: double quotes, 4 spacje, 2 puste linie przed klasa, brak nieuzywanych importow.
    Importuj tylko `from django.db import models`.
  </action>
  <verify>
    <automated>python -c "import ast; tree = ast.parse(open('content/models.py').read()); names = [n.name for n in tree.body if isinstance(n, ast.ClassDef)]; assert 'WeeklyResearch' in names, names" && grep -q "week_label" content/models.py && grep -q "formatted_json" content/models.py && grep -q "ordering = \\[\"-date_to\"\\]" content/models.py</automated>
  </verify>
  <done>
    Model zdefiniowany ze wszystkimi 9 polami; `Meta.ordering` ustawione; `__str__` zwraca
    sformatowany label; plik parsuje sie bez bledow skladniowych.
  </done>
</task>

<task type="auto">
  <name>Task 3: Admin dla `WeeklyResearch`</name>
  <files>content/admin.py</files>
  <action>
    Utworz `content/admin.py` rejestrujacy `WeeklyResearch` przez dekorator `@admin.register`:

    ```python
    from django.contrib import admin

    from .models import WeeklyResearch


    @admin.register(WeeklyResearch)
    class WeeklyResearchAdmin(admin.ModelAdmin):
        list_display = ("week_label", "date_from", "date_to", "status", "created_at")
        list_filter = ("status",)
        search_fields = ("week_label",)
        readonly_fields = ("raw_research", "formatted_json", "created_at", "updated_at")
        ordering = ("-date_to",)
    ```

    Styl: double quotes, dekorator `@admin.register` (tak jak w `accounts/admin.py` linia 33),
    bez actions w v1.
  </action>
  <verify>
    <automated>grep -q "@admin.register(WeeklyResearch)" content/admin.py && grep -q "list_display" content/admin.py && grep -q "readonly_fields" content/admin.py && python -c "import ast; ast.parse(open('content/admin.py').read())"</automated>
  </verify>
  <done>
    Admin zarejestrowany przez dekorator; `list_display`, `list_filter`, `search_fields`,
    `readonly_fields`, `ordering` skonfigurowane; plik parsuje sie bez bledow.
  </done>
</task>

<task type="auto">
  <name>Task 4: Management command `run_weekly_research` (verbatim prompts + 2 calls)</name>
  <files>content/management/commands/run_weekly_research.py</files>
  <action>
    Utworz komende `run_weekly_research`:

    1. **Importy** (na gorze pliku):
       ```python
       import json
       from datetime import date, timedelta

       from django.conf import settings
       from django.core.management.base import BaseCommand, CommandError

       from content.models import WeeklyResearch
       ```

    2. **Module-level constants** `RESEARCH_PROMPT` i `FORMAT_PROMPT` — **SKOPIUJ DOSLOWNIE** z
       `.planning/quick/260602-i1r-nowy-app-content-z-weekly-research-pipel/PROMPTS-VERBATIM.md`.
       Nie parafrazuj, nie tlumacz, nie skracaj. Paleta kolorow w FORMAT_PROMPT jest juz
       dostosowana do marki (`#f3ead7`, `#6b7a3a`, `#b6562e`, `#c89a3a`, `#2a2420`) — zostaw
       tak jak jest w pliku verbatim. NIE podwajaj klamer w przykladzie JSON-a w FORMAT_PROMPT
       (uzyjemy `.replace()` zamiast `.format()` dla call 2 — patrz nizej).

    3. **Class Command(BaseCommand)**:
       - `help = "Uruchamia tygodniowy research + format na contentu (call 1 + call 2 do Anthropic)."`

       - `add_arguments(self, parser)`:
         ```python
         parser.add_argument(
             "--force",
             action="store_true",
             help="Wymus ponowne wygenerowanie nawet jesli status to 'formatted'.",
         )
         ```

       - `handle(self, *args, **options)`:
         a) **Wyznacz zakres dat (poprzedni tydzien ISO)**:
            ```python
            today = date.today()
            # Poniedzialek tego tygodnia:
            current_monday = today - timedelta(days=today.weekday())
            # Niedziela poprzedniego tygodnia = current_monday - 1 dzien:
            date_to = current_monday - timedelta(days=1)
            # Poniedzialek poprzedniego tygodnia:
            date_from = date_to - timedelta(days=6)
            # ISO week label dla daty z poprzedniego tygodnia (date_to nalezy do poprzedniego):
            iso_year, iso_week, _ = date_to.isocalendar()
            week_label = f"{iso_year}-W{iso_week:02d}"
            ```

         b) **Idempotency check**:
            ```python
            existing = WeeklyResearch.objects.filter(week_label=week_label).first()
            if existing and existing.status == "formatted" and not options["force"]:
                self.stdout.write(
                    self.style.WARNING(
                        f"WeeklyResearch dla {week_label} juz istnieje ze statusem 'formatted'. "
                        f"Pomijam (uzyj --force aby nadpisac)."
                    )
                )
                return
            ```

         c) **API key check**:
            ```python
            api_key = getattr(settings, "ANTHROPIC_API_KEY", "")
            if not api_key:
                raise CommandError(
                    "ANTHROPIC_API_KEY nie jest ustawiony (settings/.env)."
                )
            ```

         d) **Get-or-create row** z resetem stanu jesli --force lub istnial failed:
            ```python
            row, _ = WeeklyResearch.objects.get_or_create(
                week_label=week_label,
                defaults={
                    "date_from": date_from,
                    "date_to": date_to,
                    "status": "pending",
                },
            )
            # Reset stanu przy --force lub po failed:
            row.date_from = date_from
            row.date_to = date_to
            row.status = "pending"
            row.error_message = ""
            row.save(update_fields=["date_from", "date_to", "status", "error_message", "updated_at"])
            ```

         e) **Lazy import anthropic** (po API key check, zeby `--help` dzialalo bez zainstalowanego SDK):
            ```python
            try:
                from anthropic import Anthropic
            except ImportError as exc:
                raise CommandError(
                    "Brak pakietu `anthropic`. Zainstaluj: pip install anthropic"
                ) from exc

            client = Anthropic(api_key=api_key)
            ```

         f) **Call 1 — research z web search**:
            ```python
            self.stdout.write(self.style.NOTICE(
                f"[1/2] Research dla {week_label} ({date_from} – {date_to})..."
            ))
            try:
                research_response = client.messages.create(
                    model="claude-sonnet-4-6",
                    max_tokens=8000,
                    tools=[{"type": "web_search_20250305", "name": "web_search"}],
                    messages=[
                        {
                            "role": "user",
                            "content": RESEARCH_PROMPT.format(
                                date_from=date_from.isoformat(),
                                date_to=date_to.isoformat(),
                            ),
                        }
                    ],
                )
                raw_research = "".join(
                    block.text
                    for block in research_response.content
                    if getattr(block, "type", None) == "text"
                ).strip()
                if not raw_research:
                    raise RuntimeError("Pusty research po wyfiltrowaniu blokow text.")
                row.raw_research = raw_research
                row.status = "research_done"
                row.save(update_fields=["raw_research", "status", "updated_at"])
                self.stdout.write(self.style.SUCCESS(
                    f"[1/2] OK — {len(raw_research)} znakow researchu."
                ))
            except Exception as exc:
                row.status = "failed"
                row.error_message = f"call 1 (research): {exc}"
                row.save(update_fields=["status", "error_message", "updated_at"])
                self.stderr.write(self.style.ERROR(f"[1/2] FAIL: {exc}"))
                raise
            ```

         g) **Call 2 — format do JSON-a (bez web search, uzywa replace)**:
            ```python
            self.stdout.write(self.style.NOTICE(f"[2/2] Formatowanie JSON dla {week_label}..."))
            try:
                format_message = FORMAT_PROMPT.replace("{raw_research}", raw_research)
                format_response = client.messages.create(
                    model="claude-sonnet-4-6",
                    max_tokens=8000,
                    messages=[{"role": "user", "content": format_message}],
                )
                raw_format = "".join(
                    block.text
                    for block in format_response.content
                    if getattr(block, "type", None) == "text"
                ).strip()
                # Sciagamy ewentualne fence'y markdown:
                if raw_format.startswith("```"):
                    raw_format = raw_format.split("\n", 1)[1] if "\n" in raw_format else raw_format
                    if raw_format.endswith("```"):
                        raw_format = raw_format.rsplit("```", 1)[0]
                    raw_format = raw_format.strip()
                    # Usun ewentualny prefix "json" po pierwszym fence:
                    if raw_format.startswith("json\n"):
                        raw_format = raw_format[5:].strip()
                try:
                    parsed = json.loads(raw_format)
                except json.JSONDecodeError as exc:
                    truncated = raw_format[:2000]
                    row.status = "failed"
                    row.error_message = (
                        f"call 2 (format) JSON parse error: {exc}. "
                        f"Raw (truncated 2000): {truncated}"
                    )
                    row.save(update_fields=["status", "error_message", "updated_at"])
                    self.stderr.write(self.style.ERROR(f"[2/2] FAIL: nie udalo sie sparsowac JSON: {exc}"))
                    raise CommandError("Format JSON parse error.") from exc
                row.formatted_json = parsed
                row.status = "formatted"
                row.save(update_fields=["formatted_json", "status", "updated_at"])
                self.stdout.write(self.style.SUCCESS(
                    f"[2/2] OK — JSON zapisany ({len(raw_format)} znakow surowych)."
                ))
            except CommandError:
                raise
            except Exception as exc:
                row.status = "failed"
                row.error_message = f"call 2 (format): {exc}"
                row.save(update_fields=["status", "error_message", "updated_at"])
                self.stderr.write(self.style.ERROR(f"[2/2] FAIL: {exc}"))
                raise
            ```

         h) **Komunikat koncowy**:
            ```python
            self.stdout.write(self.style.SUCCESS(f"Pipeline ukonczony dla {week_label}."))
            ```

    4. **Verbatim check**: po dokonczeniu pliku porownaj wizualnie RESEARCH_PROMPT i FORMAT_PROMPT
       z `PROMPTS-VERBATIM.md`. Kazda roznica (poza tym ze masz `RESEARCH_PROMPT = """...` /
       `FORMAT_PROMPT = """..."""` przypisanie) to blad.

    UWAGA — uzywamy `RESEARCH_PROMPT.format(date_from=..., date_to=...)` dla call 1 (brak innych
    nawiasow klamrowych w tym promcie). Dla call 2 uzywamy `FORMAT_PROMPT.replace("{raw_research}", ...)`
    bo FORMAT_PROMPT zawiera literalne klamry JSON-a — DLATEGO w pliku verbatim **nie podwajaj** klamer.
  </action>
  <verify>
    <automated>python -c "import ast; tree = ast.parse(open('content/management/commands/run_weekly_research.py').read()); classes = [n.name for n in tree.body if isinstance(n, ast.ClassDef)]; assert 'Command' in classes, classes; assigns = [n.targets[0].id for n in tree.body if isinstance(n, ast.Assign) and isinstance(n.targets[0], ast.Name)]; assert 'RESEARCH_PROMPT' in assigns and 'FORMAT_PROMPT' in assigns, assigns" && grep -q "web_search_20250305" content/management/commands/run_weekly_research.py && grep -q "claude-sonnet-4-6" content/management/commands/run_weekly_research.py && grep -q "FORMAT_PROMPT.replace" content/management/commands/run_weekly_research.py</automated>
  </verify>
  <done>
    Plik komendy parsuje sie; `Command`, `RESEARCH_PROMPT`, `FORMAT_PROMPT` zdefiniowane na
    poziomie modulu; web search tool name `web_search_20250305`; model `claude-sonnet-4-6`;
    `--force` zarejestrowane; idempotency check + API key check + 2 calls + status/error_message
    persist na kazdym kroku.
  </done>
</task>

<task type="auto">
  <name>Task 5: Dodaj `content` do INSTALLED_APPS + ANTHROPIC_API_KEY w settings + anthropic w requirements</name>
  <files>backend/settings.py, requirements.txt</files>
  <action>
    **`backend/settings.py`**:
    - Do listy `INSTALLED_APPS` (linie 58-79) dodaj `"content",` na koncu listy lokalnych appow,
      tuz po `"newsletter",`. Zachowaj kolejnosc/formatowanie (double quotes, przecinek na koncu).
    - Pod koniec pliku, w sekcji wraz z innymi env varami (po sekcji Email, przed sekcja Logging
      lub na samym koncu — wybierz miejsce ktore pasuje do struktury), dodaj blok:
      ```python
      # Anthropic (weekly research pipeline — content app)
      ANTHROPIC_API_KEY = env("ANTHROPIC_API_KEY", default="")
      ```
      UWAGA: settings juz uzywa `django-environ` (`env = environ.Env(...)`), wiec uzywamy `env(...)`,
      NIE `os.environ.get(...)`. Brief pierwotnie sugerowal `os.environ.get`, ale wzorzec projektu
      to `django-environ` — uzyj `env("ANTHROPIC_API_KEY", default="")` dla spojnosci z P24_*,
      EMAIL_* itd.

    **`requirements.txt`**:
    - Dopisz na koncu pliku linie: `anthropic>=0.40,<1.0`
    - Zachowaj brak pustej linii koncowej lub dopisz zgodnie z istniejacym formatem.
  </action>
  <verify>
    <automated>grep -q "\"content\"," backend/settings.py && grep -q "ANTHROPIC_API_KEY" backend/settings.py && grep -q "^anthropic" requirements.txt && python -c "import ast; ast.parse(open('backend/settings.py').read())"</automated>
  </verify>
  <done>
    `"content"` widoczne w INSTALLED_APPS; `ANTHROPIC_API_KEY` jako setting (env-driven, default "");
    `anthropic>=0.40,<1.0` w requirements.txt; settings.py nadal parsuje sie bez bledow skladniowych.
  </done>
</task>

<task type="auto">
  <name>Task 6: Wygeneruj i zastosuj migracje + smoke test</name>
  <files>content/migrations/0001_initial.py</files>
  <action>
    1. Wygeneruj migracje:
       ```bash
       python manage.py makemigrations content
       ```
       Spodziewany rezultat: utworzony `content/migrations/0001_initial.py` z `CreateModel`
       dla `WeeklyResearch`.

    2. Zastosuj migracje:
       ```bash
       python manage.py migrate content
       ```

    3. Smoke test rejestracji komendy + integralnosci:
       ```bash
       python manage.py check
       python manage.py run_weekly_research --help
       ```
       Drugi commad MUSI wypisac sekcje `--force` (potwierdza ze argument jest zarejestrowany).

    NIE uruchamiaj `run_weekly_research` bez `--help` — brak ANTHROPIC_API_KEY spowoduje
    `CommandError`, co jest oczekiwane ale niepotrzebne tu (testujemy tylko rejestracje).

    Zatwierdz migracje do gita w tym samym commicie co PLAN-level commit (gsd-tools commit
    obejmie nowy plik 0001_initial.py automatycznie via `--files`).
  </action>
  <verify>
    <automated>python manage.py check 2>&1 | grep -qE "(System check identified no issues|0 issues)" && python manage.py run_weekly_research --help 2>&1 | grep -q -- "--force" && test -f content/migrations/0001_initial.py</automated>
  </verify>
  <done>
    Migracja `0001_initial.py` istnieje; `manage.py check` przechodzi bez bledow;
    `run_weekly_research --help` listuje `--force`; baza ma tabele `content_weeklyresearch`.
  </done>
</task>

</tasks>

<verification>
Po wszystkich taskach:
1. `python manage.py check` → 0 issues
2. `python manage.py run_weekly_research --help` → pokazuje help i flage `--force`
3. `python manage.py shell -c "from content.models import WeeklyResearch; print(WeeklyResearch.objects.count())"` → 0 (lub > 0, ale bez bledu)
4. W adminie `/admin/content/weeklyresearch/` lista jest dostepna (manualnie, opcjonalne — admin sprawdzany przez check + import)
5. Verbatim audit: `diff <(grep -A 100 "^RESEARCH_PROMPT" content/management/commands/run_weekly_research.py) <(...)` — wizualnie potwierdz ze tresc promptow zgadza sie z `PROMPTS-VERBATIM.md` (nie powinno byc zmian poza `RESEARCH_PROMPT = """...` / przypisaniem zmiennej).
</verification>

<success_criteria>
- App `content/` istnieje z `apps.py`, `models.py`, `admin.py`, migracja, struktura komend.
- Model `WeeklyResearch` ma wszystkie 9 pol + `Meta.ordering` + `__str__`.
- Admin pokazuje liste z filtrem statusu i readonly raw_research/formatted_json.
- Komenda `run_weekly_research` jest zarejestrowana, ma `--force`, robi idempotency check,
  czyta `settings.ANTHROPIC_API_KEY` i wykonuje dwa calle Anthropic (research z web search +
  format do JSON-a) z persystencja statusu na kazdym kroku.
- RESEARCH_PROMPT i FORMAT_PROMPT to **doslowne** kopie z `PROMPTS-VERBATIM.md`.
- `requirements.txt` ma `anthropic>=0.40,<1.0`.
- `backend/settings.py` ma `"content"` w INSTALLED_APPS i `ANTHROPIC_API_KEY` jako env-driven setting.
- `python manage.py check` przechodzi bez bledow.
- `python manage.py run_weekly_research --help` dziala bez API key i listuje `--force`.
- Migracja `content/migrations/0001_initial.py` istnieje i jest zaaplikowana.
- Brak wywolania Anthropic API w trakcie wykonania planu (placeholder na pozniej).
</success_criteria>

<output>
After completion, create `.planning/quick/260602-i1r-nowy-app-content-z-weekly-research-pipel/260602-i1r-01-SUMMARY.md`
zawierajace:
- Co zostalo zbudowane (app content, model, admin, komenda)
- Jakie pliki utworzone / zmienione
- Jak uruchomic: `ANTHROPIC_API_KEY=... python manage.py run_weekly_research [--force]`
- Co dalej (TODO dla uzytkownika): dodac `ANTHROPIC_API_KEY` do `.env`, opcjonalnie skonfigurowac
  cron na uruchomienie raz w tygodniu (poniedzialek rano).
</output>
