---
phase: quick-260608-e71
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [content/admin.py]
autonomous: true
requirements: [QUICK-260608-e71]
must_haves:
  truths:
    - "Promocja WeeklyResearch z poprawnym formatted_json.blog tworzy BlogPost (draft), nie ląduje w 'failed'"
    - "body BlogPosta jest zlozony z intro + sekcji (## heading + body), w formacie zgodnym z _preview.html"
    - "excerpt BlogPosta pochodzi z blog.meta_description (fallback: blog.excerpt)"
    - "Helper skladajacy body jest testowalny bez DB (czysta funkcja modulowa)"
  artifacts:
    - path: "content/admin.py"
      provides: "Naprawiona akcja promote_to_blogpost + helper _blog_body_from_json"
      contains: "_blog_body_from_json"
  key_links:
    - from: "content/admin.py:promote_to_blogpost"
      to: "content/admin.py:_blog_body_from_json"
      via: "wywolanie helpera zamiast blog.get('body')"
      pattern: "_blog_body_from_json\\(blog\\)"
---

<objective>
Napraw akcje admina `promote_to_blogpost` w `content/admin.py`. Akcja czyta nieistniejace klucze `blog.get("body")` i `blog.get("excerpt")`, przez co kazda promocja konczy sie w galezi "failed" (warunek `if not title or not body`). Schemat z FORMAT_PROMPT to faktycznie `{title, intro, sections[{heading, body}], tags, meta_description}`.

Purpose: Uzytkownik na produkcji moze realnie promowac WeeklyResearch do BlogPost (draft) — to obecnie zawsze zawodzi.
Output: Naprawiona funkcja + wydzielony testowalny helper `_blog_body_from_json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Aktualny (bugowany) kod akcji — content/admin.py linie 13-60 -->
Schemat blog dict (z FORMAT_PROMPT, run_weekly_research.py ~56-62), NIE RUSZAC:
```
{
  "title": "...",
  "intro": "1-2 akapity",
  "sections": [{"heading": "...", "body": "..."}],
  "tags": ["..."],
  "meta_description": "do 155 znakow"
}
```

Kanoniczny format markdown (z _preview.html linia 40, "Skopiuj calosc bloga"):
`# {title}\n\n{intro}\n\n## {heading}\n\n{section body}\n\n... **Tagi:** #tag ... _{meta_description}_`
UWAGA: do pola body BlogPosta NIE wkladamy title (osobne pole) ani tagow (osobne pole).
Body = intro + sekcje (## heading + body) laczone przez "\n\n", .strip().

Model BlogPost (content/models.py): title(max200), slug(auto), excerpt(TextField),
body(TextField, markdown), tags(CharField max), status, source_research(FK).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wydziel helper _blog_body_from_json i napraw odczyt body/excerpt</name>
  <files>content/admin.py</files>
  <action>
W content/admin.py dodaj na poziomie modulu (przed `promote_to_blogpost`, po importach, ~linia 11) czysta funkcje:

```python
def _blog_body_from_json(blog):
    """Skladaj Markdown body z intro + sections (schemat FORMAT_PROMPT).

    Format zgodny z _preview.html: intro jako pierwszy akapit, kazda sekcja
    jako "## {heading}\\n\\n{body}". Title i tagi NIE wchodza do body
    (to osobne pola BlogPosta). Defensywnie pomija nie-dict sekcje.
    """
    parts = []
    intro = (blog.get("intro") or "").strip()
    if intro:
        parts.append(intro)
    for section in blog.get("sections") or []:
        if not isinstance(section, dict):
            continue
        heading = (section.get("heading") or "").strip()
        section_body = (section.get("body") or "").strip()
        chunk = []
        if heading:
            chunk.append(f"## {heading}")
        if section_body:
            chunk.append(section_body)
        if chunk:
            parts.append("\n\n".join(chunk))
    body = "\n\n".join(parts).strip()
    if not body:
        # Fallback kompatybilnosci: gdyby kiedys pojawil sie plaski schemat
        body = (blog.get("body") or "").strip()
    return body
```

Nastepnie w `promote_to_blogpost` zmien dwie linie odczytu:
- Linia `body = (blog.get("body") or "").strip()` -> `body = _blog_body_from_json(blog)`
- Linia `excerpt = (blog.get("excerpt") or "").strip()` -> `excerpt = (blog.get("meta_description") or blog.get("excerpt") or "").strip()`

NIE ZMIENIAJ reszty: idempotencji (`research.blog_posts.exists()`), odczytu title,
logiki tagow (tags/hashtags, lista vs string, lstrip "#"), warunku
`if not title or not body`, limitow (`title[:200]`, `excerpt[:300]`, `tags[:300]`),
`status="draft"`, `source_research=research`, komunikatow messages.

Konwencje CLAUDE.md: double quotes dla nowych stringow, snake_case, 4 spacje wciecia.
NIE ruszaj FORMAT_PROMPT ani modelu BlogPost.
  </action>
  <verify>
    <automated>.venv/bin/python manage.py check 2>&1 | tail -5 && .venv/bin/python -c "import django; django.setup()" 2>/dev/null; DJANGO_SETTINGS_MODULE=backend.settings .venv/bin/python -c "
from content.admin import _blog_body_from_json
# Happy path: intro + sekcje
blog = {'title': 'T', 'intro': 'Wstep akapit.', 'sections': [{'heading': 'Pierwsza', 'body': 'Tresc 1.'}, {'heading': 'Druga', 'body': 'Tresc 2.'}], 'tags': ['a'], 'meta_description': 'meta'}
b = _blog_body_from_json(blog)
assert b == 'Wstep akapit.\n\n## Pierwsza\n\nTresc 1.\n\n## Druga\n\nTresc 2.', repr(b)
assert 'T' not in b.split(chr(10))[0] or b.startswith('Wstep'), 'title nie powinien byc w body'
# Defensywnie: nie-dict sekcja pomijana
assert _blog_body_from_json({'intro': 'X', 'sections': ['smieci', {'heading': 'H', 'body': 'B'}]}) == 'X\n\n## H\n\nB'
# Fallback na plaski body
assert _blog_body_from_json({'body': 'plaski'}) == 'plaski'
# Pusty wszedzie
assert _blog_body_from_json({}) == ''
# Tylko heading bez body
assert _blog_body_from_json({'sections': [{'heading': 'H'}]}) == '## H'
print('OK: all helper assertions passed')
"</automated>
  </verify>
  <done>
`manage.py check` przechodzi bez bledow; inline smoke test `_blog_body_from_json` drukuje "OK: all helper assertions passed"; akcja `promote_to_blogpost` wywoluje helper i czyta `meta_description` dla excerpt.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python manage.py check` -> 0 issues
- Helper assertions (happy path, nie-dict sekcje, fallback, pusty, sam heading) przechodza
- Grep potwierdza brak `blog.get("body")` jako jedynego zrodla body w akcji i obecnosc `_blog_body_from_json(blog)` oraz `blog.get("meta_description")`
</verification>

<success_criteria>
- Akcja promote_to_blogpost tworzy BlogPost (draft) dla researchu z poprawnym formatted_json.blog
- body zlozone z intro + sekcji w formacie zgodnym z _preview.html
- excerpt z meta_description (fallback excerpt)
- Helper testowalny bez DB; FORMAT_PROMPT i model BlogPost nietkniete
- Konwencje CLAUDE.md zachowane (double quotes, snake_case, 4 spacje)
</success_criteria>

<output>
After completion, create `.planning/quick/260608-e71-napraw-promote-to-blogpost-skladaj-body-/260608-e71-SUMMARY.md`
</output>
