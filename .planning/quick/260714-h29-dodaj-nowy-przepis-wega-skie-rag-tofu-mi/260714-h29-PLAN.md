---
phase: quick-260714-h29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recipes/seed_images/weganskie-ragu-z-tofu.jpg
  - recipes/migrations/0007_add_weganskie_ragu.py
autonomous: true
requirements: [QUICK-H29]
must_haves:
  truths:
    - "Nowy przepis 'Wegańskie ragù z tofu' pojawia się w katalogu przepisów po migracji"
    - "Przepis ma zdjęcie penne z ragù (skopiowane do media z seed_images)"
    - "Migracja jest odwracalna (reverse usuwa przepis i zdjęcie)"
    - "python manage.py migrate i check przechodzą bez błędów"
  artifacts:
    - path: "recipes/seed_images/weganskie-ragu-z-tofu.jpg"
      provides: "Seed image dla nowego przepisu (kopia static/img/penne-ragu.jpg)"
    - path: "recipes/migrations/0007_add_weganskie_ragu.py"
      provides: "Data migration dodająca przepis wg wzorca 0003"
      contains: "weganskie-ragu-z-tofu"
  key_links:
    - from: "recipes/migrations/0007_add_weganskie_ragu.py"
      to: "recipes/seed_images/weganskie-ragu-z-tofu.jpg"
      via: "copy_seed_image helper"
      pattern: "copy_seed_image"
    - from: "recipes/migrations/0007_add_weganskie_ragu.py"
      to: "recipes/migrations/0006_alter_recipe_description_and_more.py"
      via: "migration dependency"
      pattern: "0006"
---

<objective>
Dodaj przepis "Wegańskie ragù z tofu" do katalogu przepisów (Django app `recipes`) jako data migration wg wzorca 0003, używając istniejącego zdjęcia `static/img/penne-ragu.jpg` (skopiowanego do `recipes/seed_images/`).

Purpose: Rozbudowa katalogu przepisów o nowe danie obiadowe. Deploy push-to-main uruchomi migrację na prod.
Output: Nowy seed image + migracja 0007 (idempotentna, odwracalna).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@recipes/models.py
@recipes/migrations/0003_add_krem_z_bialej_fasoli.py

<facts>
Fakty ustalone (nie odkrywaj na nowo):
- Przepisy żyją w DB, seedowane data migrations. Zdjęcia kopiowane z `recipes/seed_images/` do MEDIA_ROOT/recipes przez helper `copy_seed_image` (identyczny jak w 0003).
- Zdjęcie źródłowe: `static/img/penne-ragu.jpg` ISTNIEJE (137069 B). KOPIUJ (nie przenoś) — home.html nadal go używa.
- Ostatnia migracja: `0006_alter_recipe_description_and_more.py`. Nowa migracja ma dependency `("recipes", "0006_alter_recipe_description_and_more")`.
- Kategoria: `obiady` (Category slug istnieje z seedu 0002). Pobierz przez `Category.objects.get(slug="obiady")`.
- Tagi: `tofu` i `pieczone` (Tag sluginy istnieją z seedu 0004). Pobierz przez `Tag.objects.get(slug=...)` i ustaw M2M przez `recipe.tags.set([...])` PO update_or_create.
- WAŻNE — polskie znaki: 0003 używa ASCII, ale 0005 przywrócił polskie diakrytyki. Nowa migracja ma używać polskich znaków WPROST w treści (jak w LOCKED content poniżej) — bez późniejszej migracji korygującej.
- Pola Recipe do ustawienia: title, slug, category, tags(M2M), description, ingredients_text (linia=składnik), steps_text (linia=krok), prep_time, servings, difficulty, notes, image, is_published=True.
</facts>

<locked_content>
slug: weganskie-ragu-z-tofu
image_name: weganskie-ragu-z-tofu.jpg
category_slug: obiady
tags: ["tofu", "pieczone"]
servings: 4
prep_time: 45
difficulty: latwy

title: Wegańskie ragù z tofu

description: Roślinne ragù na bazie pieczonego "mielonego" z tofu w aromatycznym sosie pomidorowym. Idealne do makaronu.

ingredients_text (jedna linia = jeden składnik, w tej kolejności; nagłówki sekcji jako osobne linie):
Tofu mielone:
400 g tofu naturalnego
2 łyżeczki czosnku granulowanego
2 łyżeczki cebuli suszonej
1 łyżeczka wędzonej papryki
1 łyżeczka majeranku
1 łyżeczka suszonego tymianku
1 łyżeczka suszonej natki pietruszki
2 łyżki sosu sojowego
2 łyżki oleju
2 łyżki płatków drożdżowych
Sos:
1 cebula
1 marchewka
2 ząbki czosnku
2 puszki pomidorów
sól i pieprz do smaku
makaron do podania (opcjonalnie)

steps_text (jedna linia = jeden krok):
Tofu pokrusz w dłoniach do miski, dodaj wszystkie przyprawy, sos sojowy, olej i płatki drożdżowe, dokładnie wymieszaj.
Rozłóż płasko na blaszce i piecz 20-25 minut w 180°C (termoobieg).
Na patelni podsmaż pokrojoną cebulę, startą marchewkę i posiekany czosnek, aż zmiękną.
Dodaj 2 puszki pomidorów, dopraw solą i pieprzem.
Dodaj upieczone tofu i duś około 10 minut.
Podawaj z ulubionym makaronem.

notes: Jedna porcja ragù to ok. 305 kcal (bez makaronu). Z porcją makaronu (80 g suchego) wychodzi ok. 590 kcal. Całość: ok. 1220 kcal na 4 porcje.
</locked_content>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skopiuj zdjęcie do seed_images</name>
  <files>recipes/seed_images/weganskie-ragu-z-tofu.jpg</files>
  <action>Skopiuj (nie przenoś) `static/img/penne-ragu.jpg` do `recipes/seed_images/weganskie-ragu-z-tofu.jpg` przez `cp`. Plik źródłowy MUSI pozostać na miejscu — home.html nadal używa go jako polaroida "penne z ragù".</action>
  <verify>
    <automated>test -f recipes/seed_images/weganskie-ragu-z-tofu.jpg && test -f static/img/penne-ragu.jpg && cmp static/img/penne-ragu.jpg recipes/seed_images/weganskie-ragu-z-tofu.jpg</automated>
  </verify>
  <done>Oba pliki istnieją i są identyczne (cmp zwraca kod 0).</done>
</task>

<task type="auto">
  <name>Task 2: Utwórz migrację 0007_add_weganskie_ragu.py</name>
  <files>recipes/migrations/0007_add_weganskie_ragu.py</files>
  <action>Utwórz migrację na wzorcu strukturalnym `0003_add_krem_z_bialej_fasoli.py`: helper `lines(*items)`, słownik `RECIPE`, helper `copy_seed_image(seed_dir, image_name)` (skopiuj 1:1 z 0003), funkcja forward `add_weganskie_ragu(apps, schema_editor)`, funkcja reverse `remove_weganskie_ragu(apps, schema_editor)`, klasa `Migration` z `dependencies = [("recipes", "0006_alter_recipe_description_and_more")]` oraz `operations = [migrations.RunPython(add_weganskie_ragu, remove_weganskie_ragu)]`. Zapisz plik w UTF-8.

Różnice wobec 0003 (KONIECZNE):
- Treść z `<locked_content>` z POLSKIMI DIAKRYTYKAMI wprost (bez późniejszej migracji korygującej).
- `RECIPE` zawiera dodatkowo: `"servings": 4`, `"difficulty": "latwy"`, `"notes": "..."`, `"tag_slugs": ["tofu", "pieczone"]`.
- `ingredients_text` i `steps_text` buduj przez `lines(...)` z linii z `<locked_content>` (nagłówki sekcji "Tofu mielone:" oraz "Sos:" to osobne linie w ingredients_text).
- Forward: `Category = apps.get_model("recipes", "Category")`, `Recipe = apps.get_model("recipes", "Recipe")`, `Tag = apps.get_model("recipes", "Tag")`; `category = Category.objects.get(slug="obiady")`; wywołaj `copy_seed_image`; `recipe, _ = Recipe.objects.update_or_create(slug=RECIPE["slug"], defaults={... title, category, description, ingredients_text, steps_text, prep_time, servings, difficulty, notes, image, is_published=True})`; następnie `recipe.tags.set([Tag.objects.get(slug=s) for s in RECIPE["tag_slugs"]])` PO update_or_create.
- Reverse: `recipe = Recipe.objects.filter(slug=RECIPE["slug"]).first()`; jeśli istnieje wywołaj `recipe.tags.clear()`; potem `Recipe.objects.filter(slug=RECIPE["slug"]).delete()`; usuń plik `MEDIA_ROOT/recipes/<image_name>` jeśli istnieje (jak w 0003). NIE usuwaj tagów tofu/pieczone (są współdzielone z innymi przepisami).</action>
  <verify>
    <automated>python -c "import io,ast; ast.parse(io.open('recipes/migrations/0007_add_weganskie_ragu.py',encoding='utf-8').read())" && grep -q "weganskie-ragu-z-tofu" recipes/migrations/0007_add_weganskie_ragu.py && grep -q "0006_alter_recipe_description_and_more" recipes/migrations/0007_add_weganskie_ragu.py && grep -q "granulowanego" recipes/migrations/0007_add_weganskie_ragu.py</automated>
  </verify>
  <done>Plik parsuje się jako Python, zawiera slug, dependency na 0006 oraz polską treść składników.</done>
</task>

<task type="auto">
  <name>Task 3: Zastosuj migrację i sprawdź projekt</name>
  <files>db.sqlite3</files>
  <action>Uruchom `python manage.py migrate recipes` (zastosuje 0007 na lokalnej roboczej bazie), następnie `python manage.py check`. Na końcu potwierdź, że przepis znalazł się w DB przez `python manage.py shell -c` z zapytaniem po slug. Jeśli migracja rzuci błąd (np. brak kategorii/tagu), napraw treść migracji z Task 2, nie omijaj weryfikacji.</action>
  <verify>
    <automated>python manage.py migrate recipes && python manage.py check && python manage.py shell -c "from recipes.models import Recipe; r=Recipe.objects.get(slug='weganskie-ragu-z-tofu'); print(r.title, r.category.slug, r.servings, r.difficulty, list(r.tags.values_list('slug',flat=True)), bool(r.image))"</automated>
  </verify>
  <done>migrate i check przechodzą bez błędu; shell wypisuje "Wegańskie ragù z tofu obiady 4 latwy ['pieczone', 'tofu'] True" (kolejność tagów wg ordering by name może się różnić).</done>
</task>

</tasks>

<verification>
- `recipes/seed_images/weganskie-ragu-z-tofu.jpg` istnieje i jest identyczny ze źródłem.
- `static/img/penne-ragu.jpg` NIE został usunięty ani zmieniony.
- Migracja 0007 zastosowana, `check` czysty, przepis obecny w DB z kategorią obiady, tagami tofu+pieczone, servings=4, difficulty=latwy, ustawionym image.
</verification>

<success_criteria>
- Przepis "Wegańskie ragù z tofu" widoczny w katalogu po migracji (is_published=True).
- Migracja odwracalna (reverse czyści przepis + zdjęcie, nie rusza współdzielonych tagów).
- Brak regresji: penne-ragu.jpg nadal na home.
</success_criteria>

<output>
Create `.planning/quick/260714-h29-dodaj-nowy-przepis-wega-skie-rag-tofu-mi/260714-h29-SUMMARY.md` when done.
</output>
