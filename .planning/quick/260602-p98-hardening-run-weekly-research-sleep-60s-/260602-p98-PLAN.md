---
phase: quick-260602-p98
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/management/commands/run_weekly_research.py
autonomous: true
requirements:
  - QUICK-260602-p98
must_haves:
  truths:
    - "Komenda `manage.py run_weekly_research --help` pokazuje flagę `--retry-format`."
    - "Po udanym call 1 w normalnym flow komenda emituje komunikat o sleep i czeka 60s przed call 2."
    - "Stała `JSON_STRICTNESS_ADDENDUM` istnieje na poziomie modułu i jest doklejana do promptu call 2 w runtime."
    - "Gdy call 2 zwraca niesparsowalny JSON, komenda loguje warning, czeka 60s i robi drugą próbę z tym samym promptem."
    - "Gdy obie próby call 2 padną parsem, persystowany `error_message` zawiera fragmenty obu błędów, status = failed."
    - "Z flagą `--retry-format` komenda pomija call 1, używa istniejącego `obj.raw_research` i robi tylko call 2 (z retry)."
    - "`--force` i `--retry-format` razem podnoszą CommandError ('nie ma sensu')."
    - "RESEARCH_PROMPT i FORMAT_PROMPT pozostają verbatim z PROMPTS-VERBATIM.md (długość i pierwsze 100 znaków bez zmian)."
    - "Po deploy serwer prod ma nową wersję kodu — `manage.py check` przechodzi i `--help` pokazuje `--retry-format`."
  artifacts:
    - path: "content/management/commands/run_weekly_research.py"
      provides: "Zhardenowany pipeline weekly research z sleep 60s, JSON strictness addendum, auto-retry call 2 i flagą --retry-format."
      contains: "JSON_STRICTNESS_ADDENDUM, _call_format, --retry-format"
  key_links:
    - from: "content/management/commands/run_weekly_research.py:handle()"
      to: "content/management/commands/run_weekly_research.py:_call_format()"
      via: "wywołanie helpera zamiast inline call 2 + parsowanie"
      pattern: "_call_format\\(client, prompt"
    - from: "content/management/commands/run_weekly_research.py:handle() (normal flow)"
      to: "time.sleep(60)"
      via: "po `row.status = research_done; row.save(...)` PRZED call 2"
      pattern: "time\\.sleep\\(60\\)"
    - from: "content/management/commands/run_weekly_research.py:handle() (--retry-format)"
      to: "WeeklyResearch.objects.filter(week_label=...).first()"
      via: "ścieżka która pomija call 1 i sleep 60s, idzie wprost do _call_format()"
      pattern: "options\\[\"retry_format\"\\]"
    - from: "lokalny git origin/main"
      to: "serwer panel84.mydevil.net (jem3pizze@/usr/home/jem3pizze/domains/kuchennakomitywa.pl/public_python/)"
      via: "git push + ssh exec `bash ./deploy.sh` przez paramiko"
      pattern: "deploy\\.sh"
---

<objective>
Zhardenowanie komendy `content/management/commands/run_weekly_research.py` na cztery sposoby:
1. sleep 60s między call 1 a call 2 (Tier 1 rate limit safety),
2. dopisanie runtime addendum `JSON_STRICTNESS_ADDENDUM` do FORMAT_PROMPT,
3. auto-retry call 2 (1×) na `json.JSONDecodeError` z 60s backoffem,
4. flaga `--retry-format` (mutually exclusive z `--force`), która pomija call 1 i tylko reformuje istniejący `raw_research`.

Po edytach: walidacja lokalna, commit, push, deploy na produkcję przez paramiko + `deploy.sh`, smoke test na serwerze.

Purpose: zminimalizować ryzyko zmarnowania kredytu Anthropic na powtarzanie drogiego call 1 z web searchem, gdy wystarczy retry call 2; oraz zabezpieczyć produkcyjny cron przed znanymi flakami (rate limit Tier 1, nieescape'owane cudzysłowy w polskich tekstach od Sonnet 4.6).

Output: zhardenowana komenda na lokalnym `main` ORAZ na produkcji, README opcjonalnie zaktualizowany.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@content/management/commands/run_weekly_research.py
@content/README.md
@deploy.sh
@.planning/quick/260602-i1r-nowy-app-content-z-weekly-research-pipel/PROMPTS-VERBATIM.md

<interfaces>
<!-- Z istniejącego content/management/commands/run_weekly_research.py — wzorce do zachowania -->

Imports na górze (już są):
```python
import json
from datetime import date, timedelta
from django.conf import settings
from django.core.management.base import BaseCommand, CommandError
from content.models import WeeklyResearch
```
Brakuje: `import time` — DODAĆ.

Sygnatura `handle`:
```python
def handle(self, *args, **options):
    # options["force"] istnieje już
    # po edytach: options["retry_format"] dodatkowo
```

WeeklyResearch fields używane: `week_label`, `date_from`, `date_to`, `status`, `raw_research`, `formatted_json`, `error_message`.

Statusy używane jako stringi: `"pending"`, `"research_done"`, `"formatted"`, `"failed"`.

Wzorzec stripowania markdown fence'ów (już jest w kodzie, do wyciągnięcia do helpera):
```python
if raw_format.startswith("```"):
    raw_format = raw_format.split("\n", 1)[1] if "\n" in raw_format else raw_format
    if raw_format.endswith("```"):
        raw_format = raw_format.rsplit("```", 1)[0]
    raw_format = raw_format.strip()
    if raw_format.startswith("json\n"):
        raw_format = raw_format[5:].strip()
```

Anthropic call 2 (do zachowania w helperze):
```python
format_response = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=8000,
    messages=[{"role": "user", "content": prompt}],
)
raw_format = "".join(
    block.text for block in format_response.content
    if getattr(block, "type", None) == "text"
).strip()
```
</interfaces>

<deploy_pattern>
Paramiko SSH — wzorzec dla deploy taska (systemowy python3, NIE .venv):

```python
import paramiko, sys
c = paramiko.SSHClient()
c.set_missing_host_key_policy(paramiko.AutoAddPolicy())
c.connect("panel84.mydevil.net", username="jem3pizze", password="Tak12toto.", timeout=30)
cmd = "cd /usr/home/jem3pizze/domains/kuchennakomitywa.pl/public_python && bash ./deploy.sh"
stdin, stdout, stderr = c.exec_command(cmd, timeout=300, get_pty=True)
for line in iter(stdout.readline, ""):
    sys.stdout.write(line); sys.stdout.flush()
exit_status = stdout.channel.recv_exit_status()
c.close()
sys.exit(exit_status)
```

Hasło `Tak12toto.` — kropka na końcu JEST częścią hasła.
</deploy_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refaktor call 2 do helpera `_call_format` z auto-retry na JSONDecodeError</name>
  <files>content/management/commands/run_weekly_research.py</files>
  <action>
W `content/management/commands/run_weekly_research.py`:

1. Dodaj `import time` na początku pliku (obok `import json`).

2. Dodaj `import re` (potrzebne do bezpieczniejszego stripa fence'ów — opcjonalnie; jeśli zachowujesz dotychczasowy wzorzec stringowy, pomiń).

3. Wydziel z istniejącego bloku call 2 helper jako metodę `Command._call_format(self, client, prompt: str) -> dict`. Logika:
   - Robi 1 call: `client.messages.create(model="claude-sonnet-4-6", max_tokens=8000, messages=[{"role": "user", "content": prompt}])`
   - Ekstraktuje text tym samym wzorcem co teraz (`"".join(block.text for block in ... if getattr(block, "type", None) == "text").strip()`)
   - Stripuje markdown fence'y dokładnie tym samym kodem co teraz (zachowaj 1:1 — to jest test regresji).
   - Próbuje `json.loads(text)`. Jeśli OK → `return parsed`.
   - Na `json.JSONDecodeError as exc`:
     - Loguje WARNING przez `self.stderr.write(self.style.WARNING(...))`: wypisuje `exc.pos`, `exc.lineno`, `exc.colno` i 100 znaków kontekstu wokół `exc.pos` (slice: `text[max(0, exc.pos-50):exc.pos+50]`).
     - Loguje też informację „Retry za 60s..."
     - `time.sleep(60)`
     - Robi DRUGI call z dokładnie tym samym `prompt`.
     - Stripuje fence'y tym samym kodem.
     - Próbuje `json.loads(text2)`. Jeśli OK → `return parsed2`.
     - Jeśli drugi też pada `json.JSONDecodeError as exc2`:
       - `raise CommandError(f"call 2 (format) JSON parse FAIL po 2 probach. 1: {exc} | pos={exc.pos}. 2: {exc2} | pos={exc2.pos}. Raw2 (truncated 2000): {text2[:2000]}")`

4. W `handle()`, w bloku call 2:
   - Zastąp dotychczasowy inline kod (od `format_message = ...` do `parsed = json.loads(raw_format)` + jego except handler) wywołaniem:
     ```python
     prompt = FORMAT_PROMPT.replace("{raw_research}", raw_research) + JSON_STRICTNESS_ADDENDUM
     try:
         parsed = self._call_format(client, prompt)
     except CommandError as exc:
         row.status = "failed"
         row.error_message = str(exc)
         row.save(update_fields=["status", "error_message", "updated_at"])
         self.stderr.write(self.style.ERROR(f"[2/2] FAIL: {exc}"))
         raise
     except Exception as exc:
         row.status = "failed"
         row.error_message = f"call 2 (format): {exc}"
         row.save(update_fields=["status", "error_message", "updated_at"])
         self.stderr.write(self.style.ERROR(f"[2/2] FAIL: {exc}"))
         raise
     row.formatted_json = parsed
     row.status = "formatted"
     row.save(update_fields=["formatted_json", "status", "updated_at"])
     self.stdout.write(self.style.SUCCESS(f"[2/2] OK — JSON zapisany."))
     ```
   - UWAGA: dodanie `JSON_STRICTNESS_ADDENDUM` i przekazanie go do helpera — sama stała będzie dodana w Task 2; tu zostaw `+ JSON_STRICTNESS_ADDENDUM` jako import-zależność. Jeśli wykonujesz taski sekwencyjnie i Task 2 nie został jeszcze zrobiony, najpierw zrób Task 2 LUB tymczasowo zostaw bez `+ JSON_STRICTNESS_ADDENDUM` i dodaj po Task 2. Preferowane: zrób Task 2 razem z tym (single commit).

5. NIE modyfikuj `RESEARCH_PROMPT` ani `FORMAT_PROMPT` — to verbatim z briefu.

6. NIE rób migracji (model się nie zmienił).
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && .venv/bin/python manage.py check 2>&1 | tee /tmp/p98-check.log && grep -q "0 issues" /tmp/p98-check.log</automated>
  </verify>
  <done>
- `import time` jest w pliku.
- Metoda `Command._call_format(self, client, prompt)` istnieje, zwraca `dict`.
- Helper ma logikę: parse → na JSONDecodeError 60s sleep + 1 retry → drugi fail = CommandError z fragmentami obu błędów.
- `handle()` wywołuje `self._call_format(client, prompt)` zamiast inline `json.loads`.
- `manage.py check` → 0 issues.
- `RESEARCH_PROMPT` i `FORMAT_PROMPT` niezmodyfikowane (długość i pierwsze 100 znaków bez zmian — porównaj z `.planning/quick/260602-i1r-…/PROMPTS-VERBATIM.md`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Dodaj stałą `JSON_STRICTNESS_ADDENDUM` + sleep 60s między call 1 a call 2 + flagę `--retry-format`</name>
  <files>content/management/commands/run_weekly_research.py</files>
  <action>
W `content/management/commands/run_weekly_research.py`:

1. Pod istniejącą stałą `FORMAT_PROMPT` (poza klasą, na poziomie modułu), DODAJ:
   ```python
   # Obserwowane fail-e Sonnet 4.6 — model zwraca nieescape'owane cudzysłowy
   # w polskich tekstach (np. „brownie"), co rozwala json.loads(). Dodajemy w runtime,
   # zeby nie modyfikowac VERBATIM FORMAT_PROMPT (verbatim z briefu, patrz PROMPTS-VERBATIM.md).
   JSON_STRICTNESS_ADDENDUM = (
       "\n\nKRYTYCZNE: zwroc WYLACZNIE prawidlowy, parsowalny JSON. "
       "Kazdy cudzyslow wewnatrz stringa MUSI byc escapowany jako \\\". "
       "Zadnych komentarzy, zadnego tekstu poza JSON-em."
   )
   ```
   UWAGA: `\\\"` w Python source code da literalnie `\"` w stringu, co Claude zobaczy jako instrukcję „escape jako backslash-quote".

2. W `add_arguments`, dodaj flagę `--retry-format`:
   ```python
   parser.add_argument(
       "--retry-format",
       action="store_true",
       help="Pomija call 1, uzywa istniejacego raw_research i robi tylko call 2 (format).",
   )
   ```

3. W `handle()`, na samym początku (po `self.stdout.write(...)` jeśli był / przed wyliczaniem dat — patrz niżej):
   - Walidacja konfliktu flag:
     ```python
     if options["force"] and options["retry_format"]:
         raise CommandError("--force i --retry-format jednoczesnie nie ma sensu — wybierz jedno.")
     ```

4. Restrukturyzacja `handle()` dla obsługi `--retry-format`:
   - Daty (`today`, `current_monday`, `date_to`, `date_from`, `iso_year`, `iso_week`, `week_label`) — wyliczaj zawsze (oba flow ich potrzebują).
   - Sprawdzenie `existing.status == "formatted" and not force` → tylko gdy NIE `retry_format` (retry-format świadomie wraca do raz sformatowanego rekordu, ale traktujemy go jak nadpisanie — patrz niżej).
   - Sprawdzenie `ANTHROPIC_API_KEY` — zawsze.
   - Import `from anthropic import Anthropic` i `client = Anthropic(api_key=api_key)` — zawsze.

   Następnie ROZGAŁĘZIENIE:

   **Gałąź A — `if options["retry_format"]:`**
   ```python
   row = WeeklyResearch.objects.filter(week_label=week_label).first()
   if row is None:
       raise CommandError(f"Brak rekordu WeeklyResearch dla tygodnia {week_label}, nie ma czego retry-owac.")
   if not row.raw_research:
       raise CommandError(f"raw_research dla {week_label} jest pusty, nie ma czego formatowac.")
   raw_research = row.raw_research
   # NIE resetuj date_from/date_to (zachowaj jak były)
   # NIE rob get_or_create
   # NIE rob call 1
   # NIE rob time.sleep(60)
   self.stdout.write(self.style.NOTICE(
       f"[--retry-format] Pomijam call 1, uzywam istniejacego raw_research ({len(raw_research)} znakow)."
   ))
   ```

   **Gałąź B — `else:` (normalny flow)**
   - Cały dotychczasowy blok: get_or_create + reset stanu + call 1 + status=research_done.
   - **PO** `row.status = "research_done"; row.save(...)` i `self.stdout.write(SUCCESS [1/2] OK ...)`, **PRZED** call 2:
     ```python
     self.stdout.write(self.style.NOTICE(
         "Pauza 60s przed call 2 (Tier 1 rate limit safety)..."
     ))
     time.sleep(60)
     ```

5. Następnie — wspólny blok call 2 (już zrefaktorowany w Task 1):
   - `prompt = FORMAT_PROMPT.replace("{raw_research}", raw_research) + JSON_STRICTNESS_ADDENDUM`
   - `parsed = self._call_format(client, prompt)` (z try/except jak w Task 1)
   - W gałęzi `--retry-format` NIE było `row.status = "research_done"` na świeżo, więc po sukcesie call 2 wystarczy nadpisać `formatted_json`, `status="formatted"`, `error_message=""`.

6. Dwukrotnie sprawdź, że `RESEARCH_PROMPT` i `FORMAT_PROMPT` nie zostały tknięte (tylko obok dodana nowa stała).
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && .venv/bin/python manage.py check 2>&1 | grep -q "0 issues" && .venv/bin/python manage.py run_weekly_research --help 2>&1 | grep -q "retry-format" && .venv/bin/python -c "from content.management.commands.run_weekly_research import Command, JSON_STRICTNESS_ADDENDUM, RESEARCH_PROMPT, FORMAT_PROMPT; assert 'KRYTYCZNE' in JSON_STRICTNESS_ADDENDUM; assert RESEARCH_PROMPT.startswith('\nJesteś asystentem researchowym'); assert FORMAT_PROMPT.startswith('\nJesteś redaktorem treści'); print('OK')"</automated>
  </verify>
  <done>
- Stała `JSON_STRICTNESS_ADDENDUM` istnieje na poziomie modułu, eksportowalna, zawiera słowo "KRYTYCZNE".
- `manage.py run_weekly_research --help` pokazuje flagę `--retry-format`.
- `--force` + `--retry-format` razem → CommandError (manualnie zweryfikowane mentalnie, kod walidacji obecny).
- Normalny flow: po call 1 (sukces) komenda emituje notice o sleep i wywołuje `time.sleep(60)` PRZED call 2.
- Flow `--retry-format`: pomija call 1, pomija sleep 60s, ładuje `raw_research` z bazy, idzie wprost do `_call_format()`.
- `RESEARCH_PROMPT` i `FORMAT_PROMPT` pozostają verbatim (sprawdzane przez assert pierwszych słów).
- `manage.py check` → 0 issues.
  </done>
</task>

<task type="auto">
  <name>Task 3: Aktualizacja content/README.md — usunięcie ostrzeżenia o brakującej fladze + opis retry / sleep</name>
  <files>content/README.md</files>
  <action>
W `content/README.md`:

1. W sekcji „Co zrobić, jak padło call 2" — usuń blockquote zaczynający się od `> ⚠️ \`--force\` aktualnie powtarza też call 1...` (linia ~62). Zastąp go nowym akapitem:

   ```markdown
   **Tańsza opcja — `--retry-format`:** jeśli `raw_research` w bazie jest OK, a padło tylko formatowanie, użyj:

   ```bash
   ~/.virtualenvs/komitywa/bin/python manage.py run_weekly_research --retry-format
   ```

   Komenda pomija drogi call 1 (web search), używa istniejącego `raw_research` z bazy i robi tylko call 2 (format). Koszt: ~$0.02-0.05 zamiast pełnych $0.10-0.20.

   `--retry-format` jest mutually exclusive z `--force` (nie ma sensu wymuszać call 1 i jednocześnie go pomijać).
   ```

2. W sekcji „Jak odpalić ręcznie" — pod blokiem z `--force` dodaj:

   ```markdown
   **Z `--retry-format`** — pomija call 1, robi tylko call 2 na istniejącym `raw_research`:
   ```bash
   ~/.virtualenvs/komitywa/bin/python manage.py run_weekly_research --retry-format
   ```
   ```

3. Opcjonalnie (jeśli mieści się w stylu README): w sekcji „Co to robi" pod opisem call 2 dodaj jedno zdanie:
   > Pomiędzy call 1 a call 2 komenda czeka 60s, żeby nie wpaść w rate limit Anthropic Tier 1. Na auto-retry call 2 (1×) komenda również czeka 60s przed drugą próbą.

4. NIE modyfikuj innych sekcji (statusy, koszty, cron pozostają).
  </action>
  <verify>
    <automated>grep -q "retry-format" /home/tomo/workspace/komitywa/content/README.md && ! grep -q "daj znać, dorobię" /home/tomo/workspace/komitywa/content/README.md</automated>
  </verify>
  <done>
- README zawiera instrukcję użycia `--retry-format`.
- Stara obietnica „daj znać, dorobię" usunięta.
- Wzmianka o sleep 60s obecna (rate limit safety).
  </done>
</task>

<task type="auto">
  <name>Task 4: Walidacja całościowa lokalna + sanity check promptów verbatim</name>
  <files>(brak — tylko polecenia walidacyjne)</files>
  <action>
Odpal kompletną walidację lokalną (NIE odpalaj samej komendy bez `--help`, bo zmarnuje kredyt):

1. `cd /home/tomo/workspace/komitywa && .venv/bin/python manage.py check`
   → musi zwrócić `System check identified no issues (0 silenced).`

2. `cd /home/tomo/workspace/komitywa && .venv/bin/python manage.py run_weekly_research --help`
   → musi pokazać sekcję options z `--force` ORAZ `--retry-format`.

3. Importowy smoke test:
   ```bash
   cd /home/tomo/workspace/komitywa && .venv/bin/python -c "
   from content.management.commands.run_weekly_research import (
       Command, JSON_STRICTNESS_ADDENDUM, RESEARCH_PROMPT, FORMAT_PROMPT
   )
   assert 'KRYTYCZNE' in JSON_STRICTNESS_ADDENDUM, 'brak addendum'
   assert 'Kazdy cudzyslow' in JSON_STRICTNESS_ADDENDUM, 'addendum bez instrukcji escape'
   assert hasattr(Command, '_call_format'), 'brak metody _call_format'
   print('OK')
   "
   ```

4. **Sanity check verbatim promptów** — porównaj z `.planning/quick/260602-i1r-nowy-app-content-z-weekly-research-pipel/PROMPTS-VERBATIM.md`:
   ```bash
   cd /home/tomo/workspace/komitywa && .venv/bin/python -c "
   from content.management.commands.run_weekly_research import RESEARCH_PROMPT, FORMAT_PROMPT
   print('RESEARCH_PROMPT len:', len(RESEARCH_PROMPT))
   print('RESEARCH_PROMPT first 100:', repr(RESEARCH_PROMPT[:100]))
   print('FORMAT_PROMPT len:', len(FORMAT_PROMPT))
   print('FORMAT_PROMPT first 100:', repr(FORMAT_PROMPT[:100]))
   "
   ```
   Wartości muszą się zgadzać z tym, co było w pierwotnym pliku (przed Task 1). Jeśli długość się różni → znaczy że ktoś niechcący zmodyfikował prompt; rollback i poprawić.

5. Test konfliktu flag (NIE odpala API — pada wcześniej):
   ```bash
   cd /home/tomo/workspace/komitywa && .venv/bin/python manage.py run_weekly_research --force --retry-format 2>&1 | grep -q "nie ma sensu"
   ```
   → musi grepnąć "nie ma sensu".

Jeśli któryś krok pada → fix, NIE commituj.
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && .venv/bin/python manage.py check 2>&1 | grep -q "0 issues" && .venv/bin/python manage.py run_weekly_research --help 2>&1 | grep -qE "(--retry-format|retry_format)" && .venv/bin/python manage.py run_weekly_research --force --retry-format 2>&1 | grep -q "nie ma sensu"</automated>
  </verify>
  <done>
- Wszystkie 5 kroków walidacji przeszły.
- Prompty verbatim niezmodyfikowane (długość zgodna z oryginałem).
- Konflikt `--force --retry-format` poprawnie podnosi CommandError.
  </done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit + push do origin/main</name>
  <files>content/management/commands/run_weekly_research.py, content/README.md</files>
  <action>
Wszystkie cztery hardeningowe zmiany w jednym commicie (są semantycznie spójne):

```bash
cd /home/tomo/workspace/komitywa && \
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "fix(content): hardening run_weekly_research — sleep 60s, JSON strictness, auto-retry, --retry-format flag" \
  --files content/management/commands/run_weekly_research.py content/README.md
```

Po sukcesie:
```bash
cd /home/tomo/workspace/komitywa && git push origin main
```

UWAGA:
- NIE commituj nic poza tymi dwoma plikami.
- Jeśli pre-commit hook padnie → NIE używaj --amend, zrób fix i NOWY commit.
- Sprawdź `git status` po commicie — working tree powinien być clean (poza ewentualnymi nietkniętymi `.tmp-*.jpg`, `.claude/worktrees/*`).
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && git log -1 --pretty=%s | grep -q "hardening run_weekly_research" && git rev-parse HEAD@{upstream} >/dev/null 2>&1 && [ "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" ]</automated>
  </verify>
  <done>
- Commit `fix(content): hardening run_weekly_research — sleep 60s, JSON strictness, auto-retry, --retry-format flag` na `main`.
- `origin/main` zsynchronizowany z lokalnym HEAD.
- `git status` clean (poza znanymi unrelated artefaktami).
  </done>
</task>

<task type="auto">
  <name>Task 6: Deploy na produkcję przez paramiko SSH + bash deploy.sh</name>
  <files>(brak — operacja deployment)</files>
  <action>
Użyj SYSTEMOWEGO `python3` (NIE `.venv/bin/python`), bo `paramiko` jest dostępne systemowo (na podstawie wcześniejszych quick tasków na tym repo).

Zapisz tymczasowy skrypt deploya i odpal go (NIE inline w bash heredoc — paramiko jest wrażliwe na quoting hasła z kropką):

```bash
cat > /tmp/p98_deploy.py << 'PYEOF'
import sys
import paramiko

HOST = "panel84.mydevil.net"
USER = "jem3pizze"
PWD = "Tak12toto."  # kropka JEST częścią hasła
REMOTE_DIR = "/usr/home/jem3pizze/domains/kuchennakomitywa.pl/public_python"
CMD = f"cd {REMOTE_DIR} && bash ./deploy.sh"

c = paramiko.SSHClient()
c.set_missing_host_key_policy(paramiko.AutoAddPolicy())
print(f"[deploy] Connecting to {USER}@{HOST}...", flush=True)
c.connect(HOST, username=USER, password=PWD, timeout=30, look_for_keys=False, allow_agent=False)
print(f"[deploy] Connected. Running: {CMD}", flush=True)

stdin, stdout, stderr = c.exec_command(CMD, timeout=300, get_pty=True)
for line in iter(stdout.readline, ""):
    sys.stdout.write(line)
    sys.stdout.flush()

exit_status = stdout.channel.recv_exit_status()
c.close()
print(f"[deploy] exit status: {exit_status}", flush=True)
sys.exit(exit_status)
PYEOF

python3 /tmp/p98_deploy.py
```

Oczekiwany output (z `deploy.sh`):
- `=== Deploying Kuchenna Komitywa ===`
- `Pulling latest code...` + `Already up to date.` lub fast-forward do nowego commita
- `Installing dependencies...`
- `Running migrations...` + `No migrations to apply.` (nie dodaliśmy nic)
- `Collecting static files...`
- `Clearing bytecode cache...`
- `Restarting application...` + `devil www restart` output
- `=== Deploy complete ===`
- exit status 0

Jeśli exit ≠ 0 → przeczytaj output, popraw, retry. NIE rób force push, NIE skrótkuj.

Po sukcesie:
```bash
rm -f /tmp/p98_deploy.py
```
  </action>
  <verify>
    <automated>cd /home/tomo/workspace/komitywa && test -f /tmp/p98_deploy.py || python3 -c "import paramiko; c=paramiko.SSHClient(); c.set_missing_host_key_policy(paramiko.AutoAddPolicy()); c.connect('panel84.mydevil.net', username='jem3pizze', password='Tak12toto.', timeout=30, look_for_keys=False, allow_agent=False); _,o,_=c.exec_command('cd /usr/home/jem3pizze/domains/kuchennakomitywa.pl/public_python && git log -1 --pretty=%s'); print(o.read().decode()); c.close()" | grep -q "hardening run_weekly_research"</automated>
  </verify>
  <done>
- `deploy.sh` na serwerze ukończył się z exit 0.
- HEAD na serwerze = HEAD na origin/main (commit "hardening run_weekly_research").
- `devil www restart` wykonany — aplikacja zrestartowana.
  </done>
</task>

<task type="auto">
  <name>Task 7: Smoke test na serwerze produkcyjnym</name>
  <files>(brak — operacja weryfikacyjna na zdalnym serwerze)</files>
  <action>
Sprawdź na serwerze, że nowa wersja kodu jest aktywna. UWAGA: NIE odpalaj samej komendy `run_weekly_research` (bez `--help`) — zmarnuje kredyt.

```bash
cat > /tmp/p98_smoke.py << 'PYEOF'
import sys
import paramiko

HOST = "panel84.mydevil.net"
USER = "jem3pizze"
PWD = "Tak12toto."
REMOTE_DIR = "/usr/home/jem3pizze/domains/kuchennakomitywa.pl/public_python"
VENV_PY = "/usr/home/jem3pizze/.virtualenvs/komitywa/bin/python"

checks = [
    ("manage.py check", f"cd {REMOTE_DIR} && {VENV_PY} manage.py check 2>&1"),
    ("help shows --retry-format", f"cd {REMOTE_DIR} && {VENV_PY} manage.py run_weekly_research --help 2>&1 | grep -E 'retry-format|retry_format' || echo 'MISSING retry-format flag'"),
    ("JSON_STRICTNESS_ADDENDUM importable", f"cd {REMOTE_DIR} && {VENV_PY} -c 'from content.management.commands.run_weekly_research import JSON_STRICTNESS_ADDENDUM, Command; assert hasattr(Command, \"_call_format\"); assert \"KRYTYCZNE\" in JSON_STRICTNESS_ADDENDUM; print(\"OK\")' 2>&1"),
    ("conflict --force --retry-format raises", f"cd {REMOTE_DIR} && {VENV_PY} manage.py run_weekly_research --force --retry-format 2>&1 || true"),
    ("git HEAD subject", f"cd {REMOTE_DIR} && git log -1 --pretty=%s"),
]

c = paramiko.SSHClient()
c.set_missing_host_key_policy(paramiko.AutoAddPolicy())
c.connect(HOST, username=USER, password=PWD, timeout=30, look_for_keys=False, allow_agent=False)

failed = False
for name, cmd in checks:
    print(f"\n=== {name} ===", flush=True)
    print(f"$ {cmd}", flush=True)
    _, stdout, _ = c.exec_command(cmd, timeout=60, get_pty=True)
    output = stdout.read().decode()
    print(output, flush=True)
    exit_status = stdout.channel.recv_exit_status()
    # Niektóre testy oczekują nie-zero (np. konflikt flag), więc ważniejsza jest treść outputu.
    if name == "manage.py check" and "0 issues" not in output:
        failed = True; print(f"FAIL: {name}")
    if name == "help shows --retry-format" and "MISSING" in output:
        failed = True; print(f"FAIL: {name}")
    if name == "JSON_STRICTNESS_ADDENDUM importable" and "OK" not in output:
        failed = True; print(f"FAIL: {name}")
    if name == "conflict --force --retry-format raises" and "nie ma sensu" not in output:
        failed = True; print(f"FAIL: {name}")
    if name == "git HEAD subject" and "hardening run_weekly_research" not in output:
        failed = True; print(f"FAIL: {name}")

c.close()
sys.exit(1 if failed else 0)
PYEOF

python3 /tmp/p98_smoke.py
rm -f /tmp/p98_smoke.py
```

Wszystkie 5 check'ów musi przejść. Jeśli któryś pada — odeskaluj do user'a (NIE rób `run_weekly_research` bez `--help`).
  </action>
  <verify>
    <automated>echo "Manual: smoke output above musi pokazac: '0 issues', '--retry-format', 'OK', 'nie ma sensu', 'hardening run_weekly_research'. Jesli py3 zwrocil exit 0 -- pass."; true</automated>
  </verify>
  <done>
- `manage.py check` na serwerze → 0 issues.
- `manage.py run_weekly_research --help` → pokazuje `--retry-format`.
- Import `JSON_STRICTNESS_ADDENDUM` + `Command._call_format` działa.
- Konflikt `--force --retry-format` poprawnie podnosi CommandError z "nie ma sensu".
- `git log -1` na serwerze → commit hardening jest HEAD.
- Komenda `run_weekly_research` (bez `--help`) NIE została odpalona ani lokalnie, ani na serwerze.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → SSH prod | Hasło SSH w pamięci procesu paramiko (zapisane w skrypcie tymczasowym `/tmp/p98_deploy.py`) |
| Anthropic API ← prompt | FORMAT_PROMPT + JSON_STRICTNESS_ADDENDUM + raw_research (PII brak — research o wegańskim cukiernictwie) |
| WeeklyResearch DB ← LLM output | `formatted_json` JSON od modelu — read-only do admina, brak deserializacji do exec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p98-01 | I (Information Disclosure) | `/tmp/p98_deploy.py` zawiera hasło SSH plaintext | mitigate | `rm -f /tmp/p98_deploy.py` po deploy (jest w Task 6 i Task 7). Hasło i tak jest w CLAUDE.md / kontekście usera. |
| T-p98-02 | D (DoS) | `time.sleep(60)` × 2 wydłuża max czas wykonania komendy o 120s | accept | Cron co tydzień, nie jest ścieżką krytyczną; total max ~150s vs ~30s teraz. Akceptowalne. |
| T-p98-03 | T (Tampering) | LLM (Sonnet 4.6) zwraca payload, który po `json.loads` ląduje w `JSONField` | accept | `JSONField` nie jest exec'owany; admin renderuje tylko read-only. Brak deserializacji do obiektów Python z attributes. |
| T-p98-04 | E (Elevation) | `--retry-format` re-fetchuje `raw_research` z bazy i wysyła do Anthropic | mitigate | `raw_research` zostało zapisane przez tę samą komendę w poprzednim runie, więc trust chain niezmieniony. Brak path do user-controlled input. |
| T-p98-05 | R (Repudiation) | Failed runs zapisują `error_message` ale bez timestampu w treści | accept | Model ma `updated_at`, log Django stderr trafia do `weekly_research.log` z timestampem od crona. Wystarczające do audytu. |
| T-p98-06 | S (Spoofing) | Konflikt `--force --retry-format` mógłby być silently zignorowany | mitigate | Eksplicytny `CommandError` na początku `handle()` (Task 2 pkt 3). |
</threat_model>

<verification>
- Lokalnie: `manage.py check` → 0 issues, `--help` pokazuje `--retry-format`, import smoke OK, konflikt flag → CommandError, prompty verbatim niezmienione.
- Na serwerze: te same 4 checki + git HEAD = commit hardening.
- Krytyczne NIE: nie odpalono `run_weekly_research` bez `--help`. Verbatim prompty (RESEARCH/FORMAT) niezmienione (różnica = bug).
</verification>

<success_criteria>
1. `content/management/commands/run_weekly_research.py` zawiera: `import time`, `JSON_STRICTNESS_ADDENDUM`, `Command._call_format(self, client, prompt)`, flagę `--retry-format`, walidację konfliktu z `--force`, `time.sleep(60)` między call 1 a call 2 w normalnym flow.
2. `RESEARCH_PROMPT` i `FORMAT_PROMPT` bez zmian (sanity: długość zgodna z PROMPTS-VERBATIM.md).
3. `content/README.md` ma akapit o `--retry-format`, bez starej obietnicy „dorobię".
4. Commit atomowy na `main`, push do `origin/main` OK.
5. `deploy.sh` na panel84.mydevil.net wykonany przez paramiko z exit 0.
6. Smoke test na serwerze: 5 check'ów pass, w tym `git log -1` pokazuje commit hardening.
7. Komenda `run_weekly_research` (bez `--help`) NIE odpalona — kredyt Anthropic nietknięty.
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/quick/260602-p98-hardening-run-weekly-research-sleep-60s-/260602-p98-SUMMARY.md` zgodny z `~/.claude/get-shit-done/templates/summary.md` — wymień każdą z 4 zmian, opisz wynik smoke testu, podaj wskazówkę dla usera: „Następnym razem gdy padnie call 2 → użyj `--retry-format` zamiast `--force` (10× taniej)."
</output>
